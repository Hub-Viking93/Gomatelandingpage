# GoMate v2 — Replit Agent Prompt Playbook

This file is prompts. Paste each block into Replit Agent in order. Don't paraphrase. Don't shorten. Replit Agent needs the verbose context.

There are 41 prompts grouped in 9 phases. Each prompt is focused on a single deliverable. The Spine + Execution Waves section below tells you which 24 are the minimum viable path and which 17 are optional polish. Pre-flight checklist + story arc reference live at the end. Story arc is for you, not Replit.

After each prompt completes, BEFORE pasting the next one, run a quick verification: ask Replit Agent to confirm what it built and what's still pending. If it says "I'll come back to that later" — push back, force completion before moving on.

The lifecycle these prompts deliver:
- Stage 1 collecting → user fills profile via mascot onboarding (multi-agent extraction)
- Profile complete → user clicks "Generate my plan" → Stage 2 generating runs (multi-agent research with visible panels)
- Research complete → Stage 3 ready_for_pre_departure → user clicks "Generate my pre-departure checklist" → Stage 4 pre_departure (timeline of actions weeks-before-move)
- User does pre-departure work → clicks "I have arrived" when they land → Stage 5 arrived (settling-in DAG generated, post-arrival chat with smart suggestions)

Two manual generation triggers: research generation, pre-departure generation. This gives the user control + creates natural waypoints where users see each phase fire.

GoMate is not a "demo" or a "proof of concept" — it is an operating product with real users on a waitlist. This buildathon is fixing existing bugs and shifting positioning from "Relocation Assistant" (we help you find info) to "AI Relocation Consultant" (we replace the $5–15k human consultant). Do not call any part of this work a demo in commits, copy, or comments.

---

# THE SPINE — locked user journey (sits on the wall)

Every prompt is judged against this question: "Does this make the Spine stronger, or distract from it?" If it distracts, cut it.

The Spine is the 10-step user journey that the buildathon submission video walks through and the judges experience:

1. Land on `/` — see the new positioning copy
2. Click "Start your relocation plan"
3. Enter bundled answer in the mascot UI ("My name is Axel, I'm 30, moving from Germany to Sweden for a 14-month work posting with my wife and two kids")
4. Watch real-time extraction populate the dashboard with audit icons appearing as fields fill
5. Confirm or fill one missing field via the Question Director
6. Click "Generate my plan"
7. Agent panels populate live — six specialists working in parallel, dispatch logic visible (Visa Specialist, Tax Strategist, Cost-of-Living Analyst, Housing Researcher, Cultural Adapter, Documents Specialist)
8. Open the relocation plan with citations linking to Migrationsverket, Skatteverket, and other official sources
9. Click an audit icon → see the chain of evidence (which agent found this, when, from which URL)
10. Click "Generate my pre-departure checklist" → see consultant-grade timeline (apostille deadlines, A1 certificate, document templates, scheduled reminders) — the proof that GoMate replaces a consultant, not just a search tool

Total runtime: ~90 seconds. This is what the submission video records and what the judges walk through.

The marketing pitch — "Replace your $8,000+ relocation consultant for $39/month" — does NOT live inside the operating product. It belongs on gomaterelocate.com (homepage hero, pricing page, /why-gomate). The in-app `/compare` page (built by Prompt 9.1) is for architecture transparency only (single-agent bug vs multi-agent fix), not value-prop marketing.

---

# EXECUTION WAVES — minimum viable path to ship the Spine

The full doc has 33 prompts. The Spine is delivered by this minimum 22-prompt path. Estimated 14–18h, leaving 6–10h of buffer.

**Wave 1 — Foundation (prompts 1.1–1.5)**
Read codebase, extend schema (with soft-required enforcement per 1.2), add lifecycle stages, run migrations, build agent infra.

**Wave 2 — Onboarding (prompts 2.1–2.7)**
Build Extractor, Validator, Profile Writer, Question Director, mascot UI, onboarding page. **2.7 is the critical gate — the bundled-extraction bug must be fixed before proceeding.**

**Wave 3 — Multi-agent research (prompts 3.1, 3.2, 3.5, 3.6)**
Coordinator, six always-run specialists, orchestration with visible agent panels, end-to-end test with multiple profiles.

**Wave 4 — Plan delivery (prompts 4.1, 6.1, 6.2, 4.3 — note this order)**
Profile-aware dashboard layout, then Guide Composer, then citation rendering (externally visible — do this before audit), then audit trail popover. Citations are user-facing value; audit is internal proof. Bake the externally visible part first.

**Wave 5 — Repositioning + consultant-team visibility + free + polish (prompts 9.1, 9.8, 9.9, 9.10)**
Build the v1-vs-v2 architecture transparency page (`/compare`). Add the team-activity-feed (9.8) and "This week's priorities" card (9.9) — these two are the consultant-positioning payoff that turns dashboard from reactive to proactive. Set features free (9.10). Final polish pass against the full Spine.

## Optional stack — if Wave 5 ships with time to spare

Add in this order. Stop adding when the Spine starts feeling fragile.

1. **9.2 Agent debug drawer** — 1h. Cheap Use-of-Agent proof layer.
2. **3.4 Synthesizer + Critic** — 1–2h. Strengthens the agent narrative; catches gaps across specialists.
3. **9.4 Design overhaul** — 3–4h. ONLY start if the core is stable. Half-done premium design looks worse than no design pass.
4. **3.3 Conditional specialists** — 2–3h. Prioritize Posted Worker and Family Reunion first.
5. **5.1–5.4 Pre-departure phase** — 3–4h. Major product feature. Cuttable if time runs short, but the Spine ends at the pre-departure checklist, so if you cut this, Spine step 10 changes to "navigate the relocation plan + audit trail across the dashboard" instead.
6. Everything else (4.2, 6.3, 7.1–7.2, 8.1, 9.3, 9.5, 9.6, 9.7) — only after all the above is solid. Most of this is post-buildathon roadmap material.

---

# STABILIZE CHECKPOINT — paste between every wave

After completing each wave, paste this block as its own message into Replit Agent BEFORE starting the next wave's prompts:

```
Do not add new features. Do not refactor. Do not "improve" anything.

Verify the Spine end-to-end:
1. Walk steps 1 through N of the Spine in a fresh browser session (where N is the highest step the current wave should have unlocked).
2. List every failing route, component, API call, type error, database issue, broken state transition, or console error encountered.
3. List every Spine step that worked but felt wrong (slow, confusing, wrong copy, wrong layout).
4. Fix ONLY those issues. Do not introduce new functionality.
5. Walk the Spine again from step 1. Confirm 0 errors.

Do not proceed to the next wave until this passes cleanly. Stability over scope.
```

This stops Replit Agent from layering new code on top of accumulating bugs.

---

# PHASE 1 — Foundation (read codebase, set up infrastructure)

## Prompt 1.1 — Orientation, read codebase, comprehensive v1 audit

```
This is GoMate. It's a relocation intelligence platform I've been building for months. Read CLAUDE.md, then docs/audits/backend-audit.md, then lib/gomate/profile-schema.ts, then app/api/chat/route.ts, then app/(app)/dashboard/page.tsx, then lib/gomate/official-sources.ts, then lib/gomate/system-prompt.ts. Get a full mental model.

We are transforming v1 into v2 over the next 24 hours. v2 is not a guide platform. v2 is a Relocation Agency OS. The user has an agent team working their entire move lifecycle — pre-departure preparation, arrival week priorities, post-arrival settlement. Every step has a clear next action, a deadline, an official-source citation, and tracking.

We do not file applications on the user's behalf. We do not sign leases. But we prepare every piece of paperwork so thoroughly that submission is a 5-minute task. We tell users the exact form, where to download, what to attach, where to submit, when to submit, processing time. We pre-fill drafts based on profile data. We track deadlines. This is what real Swedish relocation agencies do (Prime Relocation, Sweden Relocators, Nimmersion, Key Relocation, NRG, Hej Relocation, Alfa Mobility) — minus the parts only humans can do.

Architecture principle: kitchen with one head chef and line cooks. One AI doing four jobs at once is the source of v1's hallucination bug. We split into focused agents with strict workspaces. Coordinator head chef per workflow. Specialists each have one tiny job.

The complete v1 audit:

API routes (30 existing in app/api/):
KEEP untouched: account/delete, account/export, banking-wizard, chat/history, checklist-progress, cost-of-living, documents, guides, guides/[id], images/destination, plan-checks, plans, profile (will get audit data added), progress (will get Question-Director-driven status), research/checklist, research/local-requirements, research/tax, research/visa, settling-in/[id], settling-in/[id]/why-it-matters, settling-in/arrive, settling-in/export-ical, settling-in (list), tax-guide, visa-tracker, wellbeing.
TRANSFORM: chat (move existing logic to chat-v1; new multi-agent orchestration when collecting; polished post-arrival when arrived), research/trigger (replace hardcoded 3-research with adaptive Coordinator), settling-in/generate (multi-agent specialists), subscription (set all features free for buildathon, don't remove tier system), guides/[id] (add citation rendering).
ADD new: pre-departure (timeline data), agent-audit (audit trail data), comparison (v1 vs v2 extraction), agents/[name]/status (server-sent events for visible agent panels).

Pages (10 existing in app/(app)/):
KEEP: banking, settings, settling-in, tax-registration, visa-tracker, guides, documents.
TRANSFORM: chat (mascot UI when collecting; polished traditional chat when arrived), dashboard (profile-aware dynamic rendering with shouldShow predicates and audit icons), guides/[id] (inline citations and per-section sources panel).
ADD new: chat-v1 (moved old onboarding chat), onboarding (mascot-based, replaces /chat for collecting), compare (side-by-side v1 vs v2), research (live agent panels during research), pre-departure (timeline view).

Components (39 existing in components/):
KEEP without changes: most of them. The full list is: affordability-card, arrival-banner, budget-card, budget-plan-card, commonly-forgotten-section, compliance-alerts, compliance-calendar, compliance-timeline, confetti, cost-of-living-card, countdown-timer, country-card, country-flag, empty-state, info-card, local-requirements-card, page-header, plan-change-summary, plan-consistency-alerts, plan-switcher, profile-summary-card, settling-in-task-card, skeletons, stat-card, theme-provider, visa-deadline-card, visa-document-checklist, visa-research-card, visa-routes-card, visa-status-badge, visa-status-stepper, wellbeing-checkin.
TRANSFORM: gomate-avatar (becomes mascot with 7 animation states), guide-section (add inline citation rendering), document-progress-card (wire in Documents Specialist apostille/translation/pre-fill data), interactive-document-checklist (same), legal-disclaimer (update copy), pre-move-timeline (extend significantly for full pre-departure phase), profile-details-card (add audit icons), source-card (becomes per-section sources panel), tax-overview-card (wire in Tax Strategist output), tier-gate (all tiers free), upgrade-modal (repurpose).
ADD new: mascot, speech-bubble, agent-panel, agent-panel-grid, audit-trail-popover, audit-icon, citation-marker, sources-panel, pre-departure-timeline, smart-suggestion-chips, comparison-view, agent-debug-drawer, profile-aware-dashboard layout, schools-card, pet-relocation-card, income-compliance-card, family-reunion-card, departure-tax-card, vehicle-import-card, property-purchase-card, posted-worker-card, trailing-spouse-card, chronic-health-card, prior-visa-history-card, agent-status-indicator.

Lib modules (40+ existing in lib/gomate/):
KEEP without changes: affordability, checklist-generator, cost-of-living-estimator, country-flags, country-name-normalizer, currency, dag-validator, exchange-rate, fetch-with-retry, image-service, index, numbeo-scraper, official-sources (THE MOAT - 195+ countries × 6 official source URL categories - never touch), pdf-generator, plan-consistency, plan-diff, plan-factory, post-arrival, profile-summary, source-linker, state-machine, supabase-utils, text-parsers, types/document-status, visa-advisor, visa-checker, visa-fallback, visa-recommendations.
TRANSFORM: profile-schema (add 30+ agency-grade fields - covered in Prompt 1.2), core-state (add pre_departure and ready_for_pre_departure stages), dashboard-state (compute card visibility from profile), guide-enrichment (replace single-LLM with Composer + section writers), guide-generator (skeleton stays, enrichment hook calls multi-agent Composer), settling-in-generator (replace single-LLM DAG with multi-agent specialists), system-prompt (keep buildPostArrivalSystemPrompt, replace giant onboarding prompt with focused per-agent prompts), progress (Question-Director-driven), tier (all features free), usage-guard (loosen limits since unlimited credits), web-research and research-* (refactor as agent tools).
ADD new: lib/agents/ directory (entire agent system), lib/gomate/pre-departure.ts (timeline computation).

Database tables (existing): all migrations stay.
ADD migrations: agent_audit, pre_departure_actions, guide_section_citations, agent_run_log.

Stages we're adding to core-state.ts: pre_departure (between complete and arrived), ready_for_pre_departure (transitional state after research finishes, before user clicks "Generate my pre-departure checklist").

We have unlimited Replit credits, all of Replit's models (Claude Sonnet 4.5/4.6/Opus 4.7, Haiku 4.5, GPT-5, Gemini 2.5/3), and Power Agents with massive context windows. Different agents get routed to different models based on what they're good at.

Before you write any code, do this:
1. Read the files I listed. Confirm what you understand about the current architecture.
2. Tell me how you intend to organize the lib/agents/ directory.
3. Confirm you understand we transform v1 in place — we do not start from scratch. v1 modules either stay, transform, or coexist with new ones.
4. Confirm you understand the lifecycle: collecting → generating (manual trigger) → ready_for_pre_departure → pre_departure (manual trigger) → arrived (manual trigger after move).
5. Confirm you will not delete v1 chat/research/enrichment/settling-in code — we move existing logic to v1 paths (chat-v1, etc.) and build new paths alongside.

Don't write any code yet. Read, plan, propose. I'll respond.
```

---

## Prompt 1.2 — Extend the profile schema with agency-grade fields

```
We need to extend lib/gomate/profile-schema.ts with new fields that real relocation agencies ask about. The current 65 fields cover the basics. Add the following.

Posted-worker / corporate-posting fields (required when purpose=work AND duration < 24 months):
- posting_or_secondment (yes/no): "Is this a corporate posting from your current employer?"
- home_country_employer (string): employer name in origin country
- posting_employer_address (string): for posted worker declarations
- posting_duration_months (number): for A1 certificate (max 24 months in EU)
- a1_certificate_status: "not_started" | "in_progress" | "obtained" | "not_applicable" - A1 proves social security continuity in EU postings
- coc_status: "not_started" | "in_progress" | "obtained" | "not_applicable" - Certificate of Coverage for non-EU bilateral treaties (US-Sweden, India-Sweden, etc.)
- pwd_filed: yes/no - Posted Worker Declaration filed with destination labor authority before work starts (Arbetsmiljöverket in Sweden, ZOLL in Germany)

Prior immigration history (always asked when prior_visa=yes):
- prior_visa_country (string)
- prior_visa_type (string)
- prior_residence_outside_origin (string): countries lived in for 6+ months ever

Document logistics (always asked):
- birth_certificate_apostille_status: "not_needed" | "in_progress" | "obtained"
- marriage_certificate_apostille_status: same (only if married)
- diploma_apostille_status: same (for skilled visa categories)
- police_clearance_status: "not_needed" | "applied" | "obtained"
- medical_exam_required: derived from destination + visa type

Trailing spouse / dependent info (when spouse_joining=yes):
- spouse_career_field (string)
- spouse_seeking_work (yes/no)
- spouse_language_skills (low/medium/high)
- spouse_visa_dependency (string): how their visa derives from primary's

Children specifics (when children_count > 0):
- children_school_type_preference: "public" | "private" | "international" | "no_preference"
- children_language_skills_destination: low/medium/high
- children_special_needs (free text or none)
- children_birth_certificate_apostille_status: same as parent's

Healthcare specifics (when healthcare_needs ≠ none):
- chronic_condition_description (free text)
- prescription_medications (yes/no, with note that some are restricted in destination countries)
- english_speaking_doctor_required (yes/no)

Pet specifics (when pets ≠ none):
- pet_microchip_status (yes/no)
- pet_vaccination_status: "current" | "outdated" | "starting"
- pet_size_weight (string)
- pet_breed (string) - some destinations restrict breeds
- pet_age (string)

Vehicle import:
- bringing_vehicle (yes/no) - core field
- vehicle_make_model_year (string, only if bringing_vehicle=yes)
- vehicle_origin_country (string)
- vehicle_emission_standard (string) - for EU import compliance

Property / housing intent:
- home_purchase_intent (yes/no): vs renting
- rental_budget_max (string): separate from monthly_budget
- furnished_preference: "furnished" | "unfurnished" | "either"
- commute_tolerance_minutes (number)
- accessibility_needs (free text)

Departure-side (when origin has exit tax — Sweden, US, Norway, Eritrea, etc.):
- departure_tax_filing_required (yes/no, derived from origin)
- exit_tax_obligations (free text, populated by Tax Strategist agent)
- pension_continuity_required (yes/no)

Goods shipping (always asked, conditional on bringing things):
- bringing_personal_effects (yes/no)
- estimated_goods_volume_cubic_meters (number, only if bringing_personal_effects=yes)
- goods_shipping_method ("air_freight" | "sea_freight" | "land" | "self_carry")
- need_storage (yes/no — short-term storage during transition)

Origin departure logistics (always asked):
- origin_lease_status ("owning" | "renting" | "neither")
- origin_lease_termination_notice_days (number, typically 30-90 — agency needs this for Departure Origin Services timeline)

Driver's license / mobility (always asked):
- driver_license_origin (yes/no)
- driver_license_destination_intent ("use_existing" | "exchange" | "convert" | "not_driving") — EU mutual recognition vs non-EU conversion paths

Working remote during transition (always asked):
- working_remote_during_transition (yes/no — affects tax residency rules during the move period)

Healthcare continuity specifics (always asked):
- prescription_medications_list (free text — for restriction checking; Adderall is illegal in Japan, certain CBD products in UAE, codeine restricted in many countries)
- pre_existing_condition_disclosure_concern (yes/no)

Religious / lifestyle continuity (always asked):
- religious_practice_required (free text or "none" — affects community matching, dietary needs, schooling considerations)

Embassy registration (always asked):
- register_with_origin_embassy_intent ("yes" | "no" | "already_registered")

Emergency contacts (always asked):
- emergency_contact_origin_name (string)
- emergency_contact_origin_phone (string)

Financial planning beyond core budget (conditional):
- pre_existing_investments_to_migrate (yes/no)
- existing_offshore_accounts (yes/no)

Insurance needs beyond healthcare (always asked):
- insurance_needs_household (yes/no)
- insurance_needs_vehicle (yes/no/n_a — n_a if not bringing vehicle)
- insurance_needs_life (yes/no)
- gap_insurance_needed (yes/no — for transition period between origin coverage end and destination coverage start)

Family visa awareness (when family_count > 0 + visa_role=primary):
- family_visa_cascade_aware (yes/no — does user understand that primary's visa must be approved first before dependent applications get easier processing)

For each field above, add a FieldConfig entry with: key, label, intent (one-line purpose), examples (2-3 example questions or values), extractionHints (terms the extractor should look for), required (boolean or predicate function checking when this field is required), category, dependsOn (which other field's value triggers this — e.g., posting_or_secondment depends on purpose=work AND duration_months < 24).

CRITICAL — soft enforcement rule for required predicates:

Enforce `required: true` ONLY for the consultant-baseline fields that gate profile completion (origin, destination, intent_type, target_arrival_window, household composition basics, partner/children counts, purpose, visa_role). All the new agency-grade fields above must be added to the schema, but they are CONDITIONAL — they only become required when explicit upstream triggers fire:

- posting_or_secondment, home_country_employer, posting_duration_months, a1_certificate_status, pwd_filed: required only if purpose=work AND posting_or_secondment=yes
- children_school_type_preference, children_language_skills_destination, children_birth_certificate_apostille_status: required only if children_count > 0
- pet_microchip_status, pet_vaccination_status, pet_breed, pet_size_weight, pet_age: required only if pets ≠ none
- chronic_condition_description, prescription_medications, english_speaking_doctor_required: required only if healthcare_needs ≠ none
- vehicle_make_model_year, vehicle_origin_country, vehicle_emission_standard: required only if bringing_vehicle=yes
- spouse_career_field, spouse_seeking_work, spouse_language_skills, spouse_visa_dependency: required only if spouse_joining=yes
- religious_practice_required, register_with_origin_embassy_intent, pre_existing_investments_to_migrate, existing_offshore_accounts: NEVER required — always optional, surfaced only if the user volunteers them

The Question Director must NEVER ask about religion, offshore accounts, pets, schools, apostille, or A1 certificates unless an upstream trigger has fired. If a field's required predicate evaluates to false, it must NOT appear in the result of getRequiredFields(). Otherwise the Question Director will ask judges about religious practice before visa type, which is catastrophic.

Add all keys to ALL_FIELDS array. Make sure FIELD_CONFIG has entries for every new field. Update getRequiredFields() in state-machine.ts so the new conditional fields trigger correctly per the rules above. Update isProfileComplete() so completion requires only the conditionally-relevant new fields (i.e. completion is reachable for a simple profile without ever filling in pet/posting/vehicle/chronic-condition fields). Update FIELD_CONFIG conditional logic accordingly.

Don't touch app/api/chat/route.ts yet. We're just extending the schema. The new fields aren't extracted yet because v2 onboarding doesn't exist yet.

Run `pnpm tsc --noEmit` after to verify no type errors.

When you're done, list every new field you added with its required predicate. Confirm the schema's conditional logic works for these scenarios:
- Solo moving for work, 14-month posting: should require posting_or_secondment, home_country_employer, posting_duration_months, a1_certificate_status, pwd_filed
- Family with kids moving permanently: should require children_school_type_preference, children_language_skills_destination, children_birth_certificate_apostille_status (per kid)
- Person with chronic condition moving with pet: should require chronic_condition_description, prescription_medications, english_speaking_doctor_required, pet_microchip_status, pet_vaccination_status, pet_breed
```

---

## Prompt 1.3 — Add new lifecycle stages to core state

```
Update lib/gomate/core-state.ts to add two new stages between complete and arrived.

Currently CanonicalPlanStage is "collecting" | "complete" | "arrived" (with archived as a status, not a stage).

Add these new stages:
- "ready_for_pre_departure" — research is done, guide is rendered. User has not yet clicked "Generate pre-departure checklist".
- "pre_departure" — user has clicked "Generate pre-departure checklist". Pre-departure timeline is generated and visible. User is doing pre-departure work over weeks.

The full lifecycle becomes: collecting → generating (existing transition state) → complete (existing, kept for backward compatibility, becomes alias for ready_for_pre_departure) → ready_for_pre_departure → pre_departure → arrived.

Update deriveCanonicalStage() to handle the new stages. The transition rules:
- collecting → generating: when isProfileComplete returns true AND user has clicked "Generate my plan" button (a new column on relocation_plans called user_triggered_research_at)
- generating → ready_for_pre_departure: when research_status = "completed" or "partial"
- ready_for_pre_departure → pre_departure: when user has clicked "Generate pre-departure checklist" button (a new column called user_triggered_pre_departure_at)
- pre_departure → arrived: when user clicks "I have arrived" (existing column arrival_date and onboarding_completed)

Update DerivedPlanAuthority interface to surface canLockPredeparture and canLockArrival booleans.

Update existing logic across the codebase that gates on stage="complete" to also accept stage="ready_for_pre_departure" or stage="pre_departure" where appropriate. Specifically check: app/api/research/trigger (researchEligible should accept ready_for_pre_departure but the user can re-run research even there), app/api/settling-in/* (should require arrived), app/api/guides/[id] (any of complete/ready_for_pre_departure/pre_departure/arrived is fine).

Add migration for the two new columns:
- relocation_plans.user_triggered_research_at (timestamp, nullable) — tracks when user clicked "Generate my plan"
- relocation_plans.user_triggered_pre_departure_at (timestamp, nullable) — tracks when user clicked "Generate my pre-departure checklist"

Don't add automatic transitions. Profile complete does NOT auto-trigger research. Research complete does NOT auto-trigger pre-departure. Each transition requires explicit user action via a button click that hits a new POST endpoint.

When you're done, walk me through the stage transitions for a typical user. I want to see the timestamps of each manual trigger.
```

---

## Prompt 1.4 — Database migrations for the four new tables

```
Create four new database migrations under supabase/migrations/. Use the timestamp format: YYYYMMDDHHMMSS_name.sql.

Migration 1: agent_audit table
- id (uuid, primary key)
- profile_id (uuid, foreign key to relocation_plans, indexed)
- agent_name (text, indexed) - e.g., "extractor", "validator", "visa-specialist"
- model_used (text) - e.g., "claude-haiku-4-5", "claude-sonnet-4-6"
- phase (text) - "extraction" | "validation" | "research" | "enrichment" | "settling-in" | "chat"
- field_or_output_key (text) - field key for extraction, section key for research, task ID for settling-in
- value (jsonb) - the value or output the agent produced
- confidence (text) - "explicit" | "inferred" | "assumed" | "full" | "partial" | "fallback"
- source_user_message (text, nullable) - for extraction agents
- source_url (text, nullable) - for research agents
- prompt_hash (text) - SHA hash of the prompt sent (for audit, not full prompt to save space)
- response_hash (text) - SHA hash of the response received
- validation_rules_applied (jsonb, nullable) - for validator outputs
- retrieved_at (timestamp)
- wall_clock_ms (integer) - how long the agent call took
- tokens_used (integer, nullable)
- retry_count (integer, default 0)

Add RLS policy: users can only see audit rows for their own profile (joined via relocation_plans.user_id).
Index: (profile_id, agent_name, retrieved_at DESC).

Migration 2: pre_departure_actions table
- id (uuid, primary key)
- profile_id (uuid, foreign key to relocation_plans, indexed)
- title (text) - e.g., "Apply for A1 certificate"
- description (text) - the why, in plain language
- weeks_before_move_start (integer) - when to start
- weeks_before_move_deadline (integer) - latest acceptable start
- estimated_duration_days (integer)
- depends_on (uuid array, nullable) - other action IDs that must complete first
- documents_needed (text array)
- official_source_url (text, nullable)
- pre_filled_form_url (text, nullable) - for actions with structured forms
- agent_who_added_it (text) - which specialist contributed this action
- legal_consequence_if_missed (text)
- status (text, default 'not_started') - "not_started" | "in_progress" | "complete" | "blocked" | "skipped"
- completed_at (timestamp, nullable)
- created_at (timestamp, default now())
- sort_order (integer)

Add RLS policy. Index: (profile_id, weeks_before_move_start).

Migration 3: guide_section_citations table
- id (uuid, primary key)
- guide_id (uuid, foreign key to existing guides table, indexed)
- section_key (text) - e.g., "visa", "budget", "housing", "documents", "posted_worker"
- paragraph_idx (integer) - position within the section
- citation_number (integer) - 1, 2, 3 etc., for [1] [2] [3] markers in the prose
- source_url (text)
- source_name (text) - human-readable e.g., "Migrationsverket - Residence permit"
- retrieved_at (timestamp)
- last_verified_at (timestamp, default = retrieved_at)
- agent_who_added_it (text) - which section writer or specialist
- created_at (timestamp, default now())

Add RLS policy (joined via guides → relocation_plans → user_id). Index: (guide_id, section_key, paragraph_idx).

Migration 4: agent_run_log table
- id (uuid, primary key)
- profile_id (uuid, foreign key to relocation_plans, indexed)
- agent_name (text)
- phase (text)
- status (text) - "started" | "completed" | "failed" | "retry"
- prompt_summary (text) - first 200 chars
- response_summary (text, nullable) - first 200 chars
- tools_called (text array)
- validation_passed (boolean, nullable)
- retry_count (integer, default 0)
- tokens_used (integer, nullable)
- wall_clock_ms (integer, nullable)
- error_message (text, nullable)
- timestamp (timestamp, default now())

Index: (profile_id, timestamp DESC). RLS policy.

Run the migrations against the local dev Supabase. Verify they apply cleanly. Then run pnpm tsc --noEmit to ensure no type errors anywhere downstream.

When you're done, show me the schema dumps for all four new tables.
```

---

## Prompt 1.5 — Build the lib/agents/ infrastructure layer

```
Set up the lib/agents/ directory and the infrastructure layer. We're not building the actual agents yet — just the foundation they'll all use.

Create lib/agents/orchestrator.ts:
- Exports a class or function called runAgentPipeline that takes a list of agent invocations and runs them with proper error handling, retries, and audit logging.
- Each agent invocation is { name, phase, run: (context) => Promise<output>, fallback?: () => output, retries?: number }.
- The orchestrator runs each agent, captures timing, writes to agent_run_log table, handles retries with exponential backoff, returns aggregated results.
- Supports both sequential and parallel agent groups (some pipelines need order, some are parallel).

Create lib/agents/audit.ts:
- Exports writeAuditRow(profile_id, agent_name, model_used, phase, field_or_output_key, value, confidence, source_user_message?, source_url?, prompt, response, validation_rules?, wall_clock_ms, tokens_used?).
- Writes to agent_audit table.
- Hashes prompt and response to save storage.
- Wraps the database insert with retry logic.

Create lib/agents/router.ts:
- Defines a model routing config:
  ```
  const AGENT_MODEL_ROUTING = {
    extractor: "claude-haiku-4-5",
    validator: null, // no LLM
    profile_writer: null, // no LLM
    question_director: "claude-sonnet-4-6",
    coordinator: "claude-opus-4-7",
    visa_specialist: "claude-sonnet-4-5",
    tax_strategist: "claude-opus-4-7",
    cost_specialist: "claude-sonnet-4-5",
    housing_specialist: "claude-sonnet-4-5",
    schools_specialist: "google/gemini-2.5-pro",
    healthcare_navigator: "claude-sonnet-4-5",
    banking_helper: "claude-sonnet-4-5",
    documents_specialist: "claude-sonnet-4-5",
    cultural_adapter: "claude-sonnet-4-6",
    pet_specialist: "claude-sonnet-4-5",
    posted_worker_specialist: "claude-sonnet-4-5",
    digital_nomad_compliance: "claude-sonnet-4-5",
    job_compliance_specialist: "claude-sonnet-4-5",
    family_reunion_specialist: "claude-sonnet-4-5",
    departure_tax_specialist: "claude-opus-4-7",
    vehicle_import_specialist: "claude-sonnet-4-5",
    property_purchase_specialist: "claude-sonnet-4-5",
    trailing_spouse_career_specialist: "claude-sonnet-4-6",
    pension_continuity_specialist: "claude-opus-4-7",
    synthesizer: "claude-opus-4-7",
    critic: "claude-sonnet-4-5",
    guide_composer: "claude-opus-4-7",
    section_writer_visa: "claude-sonnet-4-6",
    section_writer_budget: "claude-sonnet-4-6",
    section_writer_housing: "claude-sonnet-4-6",
    section_writer_banking: "claude-sonnet-4-6",
    section_writer_healthcare: "claude-sonnet-4-6",
    section_writer_culture: "claude-sonnet-4-6",
    section_writer_jobs: "claude-sonnet-4-6",
    section_writer_education: "claude-sonnet-4-6",
    section_writer_documents: "claude-sonnet-4-6",
    section_writer_posted_worker: "claude-sonnet-4-6",
    settling_in_registration: "claude-sonnet-4-5",
    settling_in_banking: "claude-sonnet-4-5",
    settling_in_housing: "claude-sonnet-4-5",
    settling_in_healthcare: "claude-sonnet-4-5",
    settling_in_employment: "claude-sonnet-4-5",
    settling_in_transport: "claude-sonnet-4-5",
    settling_in_family: "claude-sonnet-4-5",
    settling_in_tax: "claude-sonnet-4-5",
    settling_in_coordinator: "claude-opus-4-7",
    pre_departure_coordinator: "claude-opus-4-7",
    free_chat: "claude-sonnet-4-6",
  };
  ```
- Exports callLLM(agentName, prompt, options) that looks up the model and routes to OpenRouter (existing setup).
- Each call returns { content, tokens_used, wall_clock_ms }.
- Wraps existing fetch-with-retry logic.

Create lib/agents/types.ts with shared TypeScript types:
- AgentName (string union of all agent names)
- AgentInvocation { name, phase, run, fallback?, retries? }
- AgentOutput { content, confidence, source?, retrieved_at, wall_clock_ms, tokens_used? }
- ConfidenceLevel = "explicit" | "inferred" | "assumed" | "full" | "partial" | "fallback"
- AgentPhase = "extraction" | "validation" | "research" | "enrichment" | "settling-in" | "chat" | "pre-departure"

Don't build any agents yet. This is just the infrastructure they'll plug into.

Run pnpm tsc --noEmit. Confirm no type errors.

When you're done, show me the directory structure of lib/agents/ and the contents of types.ts and router.ts.
```

---

# PHASE 2 — Multi-agent onboarding

## Prompt 2.1 — Build the Extractor agent

```
Build lib/agents/extractor.ts.

The Extractor agent reads ONE user message and ONE specific field we asked them about (the pending field). It returns one of: a clean string value, a number, a yes/no, or "I couldn't tell." Plus a confidence score: explicit | inferred | assumed.

The Extractor never tries to grab fields we didn't ask about. If the user volunteers extra information ("I'm 30 and bringing my dog Max"), it flags "additional_fields_detected" with a list of field keys that were mentioned — Question Director uses this for follow-up planning.

Function signature:
```
extractField(
  userMessage: string,
  pendingField: AllFieldKey,
  lastAIMessage?: string
): Promise<ExtractionResult>

type ExtractionResult = {
  value: string | number | "yes" | "no" | null;
  confidence: "explicit" | "inferred" | "assumed";
  rawText: string; // exactly what the user typed for audit trail
  additionalFieldsDetected: AllFieldKey[]; // fields the user mentioned but we didn't ask about
  uncertaintyReason?: string; // if value is null
}
```

The system prompt should be tiny. Something like:
"You are extracting a SINGLE field from a user's message in a relocation interview.
The user was asked about: \"{config.label}\" — {config.intent}
Their response: \"{userMessage}\"
{if pendingField is yes/no field} This is a yes/no field — extract \"yes\" or \"no\" or \"unclear\".
{else} Extract the value as a concise string. For amounts include currency. For locations use \"City, Country\" format.
You may also detect if the user mentioned OTHER fields from this list: {schema field names}. List those in additionalFieldsDetected.
Return JSON: { value, confidence, additionalFieldsDetected }"

Use the model from router.ts (Haiku 4.5). Use OpenAI-compatible JSON mode. Use existing fetch-with-retry pattern.

After the LLM call, write an audit row via writeAuditRow() with the agent_name="extractor", phase="extraction", value, confidence, source_user_message, prompt and response (hashed).

If the LLM call fails entirely, return { value: null, confidence: "assumed", uncertaintyReason: "extractor_failed" }.

Test manually: import the function, call it with userMessage="I'm 30 years old and moving to Spain" and pendingField="name". Should return { value: null, confidence: ..., additionalFieldsDetected: ["birth_year", "destination"] }.

Then call with userMessage="My name is Axel" and pendingField="name". Should return { value: "Axel", confidence: "explicit" }.

Run pnpm tsc --noEmit.

When you're done, walk me through three test invocations and the audit rows they produce.
```

---

## Prompt 2.2 — Build the Validator and Profile Writer (no LLM)

```
Build lib/agents/validator.ts and lib/agents/profile-writer.ts.

Validator (lib/agents/validator.ts):
Pure code, no LLM. Takes the raw value from Extractor and the field key. Runs through schema validation.

```
validate(
  rawValue: string | number | "yes" | "no" | null,
  fieldKey: AllFieldKey
): ValidationResult

type ValidationResult =
  | { valid: true, normalizedValue: any }
  | { valid: false, errorCode: string, retryHint: string }
```

Validation rules per field type, deterministic:
- yes/no fields: normalize "yeah"/"yep"/"sure"/"definitely"/"of course"/"absolutely" → "yes"; "nah"/"nope"/"no way"/"not really" → "no". If unclear → invalid.
- birth_year: accept 4-digit year between 1900 and current year minus 16.
- date fields (target_date, etc.): parse common date formats.
- currency fields (savings_available, monthly_budget, monthly_income, rental_budget_max): accept patterns like "5000 USD", "5000 SEK", "$5000", "€5000". Normalize to "5000 USD" format.
- country fields (citizenship, destination, current_location, prior_visa_country, vehicle_origin_country): use existing country-name-normalizer.ts to validate against country list. Accept synonyms ("USA"→"United States").
- city fields (target_city): leave as-is, just trim.
- numeric fields (children_count, posting_duration_months, commute_tolerance_minutes): parse as integer.
- percentage / months fields (income_history_months, posting_duration_months): parse with units, convert to base unit (months).
- enum fields (visa_role, purpose, settlement_reason, study_type, etc.): match against allowed values.
- string fields with no constraints: accept as-is, trim.

For "I'm 30 years old" given fieldKey="birth_year": this would already have been processed by the Extractor (which returns "1995" or similar). If somehow the Extractor returned the literal string, the Validator catches it because it's not a 4-digit year.

For "I am 30" given fieldKey="name": the Validator rejects this with errorCode="not_a_name", retryHint="That sounds like an age. What's your name?"

Profile Writer (lib/agents/profile-writer.ts):
Pure code, no LLM. Takes the validated value and writes to Postgres.

```
writeProfileField(
  profile_id: string,
  fieldKey: AllFieldKey,
  validatedValue: any,
  confidence: ConfidenceLevel,
  sourceUserMessage: string,
  validationRulesApplied: Record<string, any>
): Promise<void>
```

Implementation:
1. Fetch current profile_data from relocation_plans.
2. Set profile_data[fieldKey] = validatedValue.
3. Update relocation_plans.profile_data.
4. Call writeAuditRow() with agent_name="profile_writer", phase="extraction", field_or_output_key=fieldKey, value=validatedValue, confidence, source_user_message, validation_rules_applied=validationRulesApplied.
5. Profile Writer NEVER accepts writes to fieldKeys not in the schema. If somehow called with an invalid key, throw an error before writing.

Add unit tests (or quick manual test cases) for both. Validate that:
- Extractor + Validator + Profile Writer chain handles "I'm 30 years old, moving to Sweden" correctly when the pending field is birth_year (Extractor returns "1995" inferred, Validator passes, Profile Writer writes 1995 with audit trail).
- Same chain handles the same input when the pending field is name (Extractor returns null, additionalFieldsDetected=[birth_year, destination], Validator never runs, Profile Writer never runs, Question Director gets "value couldn't be determined, additional fields mentioned").

Run pnpm tsc --noEmit.

When you're done, show me the validation logic for the currency field type and walk me through the audit rows produced for a successful extraction chain.
```

---

## Prompt 2.3 — Build the Question Director with strict schema bounding

```
Build lib/agents/question-director.ts.

The Question Director reads the current profile state and conversation history. It calls the deterministic getRequiredFields(profile) from lib/gomate/state-machine.ts to get the list of fields still needed. It picks ONE field from that list to ask next, phrases the question warmly, and returns it with an animation cue for the mascot UI.

Function signature:
```
askNext(
  profile: Profile,
  conversationHistory: Message[],
  additionalFieldsDetected?: AllFieldKey[]
): Promise<QuestionDirectorOutput>

type QuestionDirectorOutput = {
  isOnboardingComplete: boolean; // true if isProfileComplete returns true
  nextFieldKey?: AllFieldKey; // null if isOnboardingComplete=true
  questionText?: string; // the warm phrasing
  animationCue?: "idle" | "waving" | "nodding" | "smiling" | "tilting_curious" | "thinking" | "celebrating";
  shouldClarifyAdditional?: boolean; // if additionalFieldsDetected was non-empty, we should follow up on them next
  clarificationQuestion?: string; // if user mentioned extra info, ask to confirm
}
```

CRITICAL — the Question Director's system prompt MUST contain this exact constraint:

"You can ONLY ask about fields from this list: {JSON of remaining required fields with their labels and intents}. You CANNOT invent topics. You CANNOT ask about hobbies, opinions, personal preferences, or anything not on this list. If the list is empty, set isOnboardingComplete=true and do not ask another question. If a user has volunteered information about a field already on the list (additionalFieldsDetected), prioritize confirming that field next. You phrase the question warmly with country and family awareness. You don't reveal that you have a fixed list — you sound natural."

The system prompt also includes:
- The user's name if known (for personalization)
- The destination if known
- The conversation tone guidance ("warm, calm, professional")
- Examples of how to phrase questions for tricky fields:
  - For posting_or_secondment: "Quick clarification — is this a temporary assignment from your current employer, or are you taking a new role with a Swedish company?"
  - For a1_certificate_status: "For EU postings under 24 months, you'll typically apply for an A1 certificate to keep your home-country social security. Have you started that, or is it something we should help you understand?"
  - For pet specifics: "You mentioned bringing a dog — what's the breed? Some destinations have restrictions, so it's worth checking now."

Use Sonnet 4.6 from router.ts (warm conversational).

Implementation:
1. Call getRequiredFields(profile). If empty array → return { isOnboardingComplete: true }.
2. If additionalFieldsDetected has entries that are in the required fields list, prioritize them — pick the first one.
3. Otherwise pick the first field from the required list (the schema's logical ordering).
4. Build the system prompt with the constrained list.
5. Call the LLM. Parse JSON response: { questionText, animationCue, [optional] clarificationQuestion if confirming additional fields }.
6. Write audit row with agent_name="question_director", value=nextFieldKey + questionText.
7. Return.

Edge case: if Question Director's LLM somehow tries to ask about a field not in the required list, log error to agent_run_log and fall back to picking the first field from required list with a templated question ("Tell me about your {label}").

Run pnpm tsc --noEmit.

When you're done, run three test scenarios and show me the output:
1. Profile is empty (just started). Should ask about name first.
2. Profile has name=Axel, destination=Sweden, purpose=work, posting_or_secondment=yes, posting_duration_months=14. Should ask about home_country_employer or a1_certificate_status next.
3. Profile is complete (all required fields filled). Should return isOnboardingComplete=true, no question.
```

---

## Prompt 2.4 — Wire up the new chat orchestration (move v1 aside)

```
Now build the new chat orchestration that uses Extractor + Validator + Profile Writer + Question Director.

Step 1: Move the existing app/api/chat/route.ts to app/api/chat-v1/route.ts. Don't change its content. Just relocate it.

Step 2: Move the existing app/(app)/chat/page.tsx to app/(app)/chat-v1/page.tsx. Don't change its content.

Step 3: Create new app/api/chat/route.ts. This handles two modes:
- collecting/onboarding: the new multi-agent extraction flow
- arrived: the existing buildPostArrivalSystemPrompt flow (do not change behavior — just route to it)

For the collecting mode:

POST handler:
1. Auth check (existing pattern with supabase.auth.getUser()).
2. Rate limit check (existing usage-guard).
3. Get the request body: { messages: UIMessage[], profile?: Profile }.
4. Get the user's relocation_plans row to determine plan_id and stage.
5. If stage is "arrived" → use existing post-arrival logic with buildPostArrivalSystemPrompt.
6. If stage is "collecting" → run the new orchestration:
   a. Extract the last user message text (use existing getMessageText helper).
   b. Determine pending field — get from getNextPendingField(profile) on first call, or carry from previous turn.
   c. Get the last AI message for context.
   d. Run Extractor agent with userMessage, pendingField, lastAIMessage.
   e. If extractor returned { value: null }: skip ahead to Question Director with shouldClarifyAdditional logic. Don't write to profile.
   f. If extractor returned a value: run Validator. If invalid, skip to Question Director with retryHint as clarification.
   g. If validator returned valid: run Profile Writer. Profile is updated.
   h. Run Question Director with updated profile, conversation history, additionalFieldsDetected from extractor.
   i. If Question Director returns isOnboardingComplete=true: update relocation_plans.locked=true, transition stage to "complete" (DO NOT auto-trigger research — that's the user's manual action).
   j. Return the question text + animation cue + updated profile state.

Use Server-Sent Events for streaming the response so the mascot UI can show "thinking" then the question text.

Don't delete chat-v1 stuff. The comparison page will use it.

Add a new API endpoint at app/api/plans/trigger-research/route.ts that the user calls when they click "Generate my plan" button. This endpoint:
1. Verifies the user's plan is in stage="complete" with locked=true.
2. Sets user_triggered_research_at = now() on relocation_plans.
3. Triggers the research orchestration (we'll build that in Phase 3 — for now just update the timestamp).
4. Transitions stage to "generating".

When you're done, walk me through the request/response cycle for a single onboarding turn. Show me the audit rows that get written.
```

---

## Prompt 2.5 — Build the mascot UI with 7 animation states

```
Build the mascot component and the speech bubble.

components/mascot.tsx:
Friendly cartoon robot character. Use SVG-based animations or Lottie. Seven distinct animation states triggered by props:
- idle: gentle breathing animation, eyes occasionally blink
- waving: hand raises and waves (used on greeting)
- nodding: head nods up and down (used when receiving a confirmed answer)
- smiling: subtle smile expression (used when answer is good and we're moving forward)
- tilting_curious: head tilts to one side (used for clarification follow-ups)
- thinking: hand to chin or rotating gear above head (used during agent processing — should be visible for at least 1.5 seconds even if processing finishes faster)
- celebrating: confetti or arms up (used on isOnboardingComplete=true)

Component props:
```
type MascotProps = {
  state: AnimationState;
  size?: "sm" | "md" | "lg";
  className?: string;
}
```

Use Framer Motion for state transitions. Each state should have a smooth entry/exit so transitioning from "thinking" to "smiling" feels natural.

components/speech-bubble.tsx:
The speech bubble that appears next to/above the mascot.
- Renders the question text from Question Director with a typewriter or fade-in animation.
- Has a tail pointing toward the mascot.
- Style is friendly, not corporate. Soft rounded corners, gentle shadow.
- Should be readable at all sizes.
- Auto-resizes for long questions.

Component props:
```
type SpeechBubbleProps = {
  text: string;
  align?: "left" | "right" | "top";
  className?: string;
}
```

components/onboarding-input.tsx:
The input field below the mascot for the user's answer.
- Single textarea (not chat textarea — this is for THIS specific question's answer).
- Placeholder reflects the expected answer type ("Type your name", "Type 'yes' or 'no' or expand on it").
- Submit on Enter. Click submit button also works.
- Disabled with loading state while agents process.
- Has a small character count indicator (subtle).

Component props:
```
type OnboardingInputProps = {
  expectedAnswerType: "text" | "yes_no" | "number" | "currency" | "country" | "free";
  pendingFieldLabel: string;
  onSubmit: (text: string) => void;
  disabled?: boolean;
}
```

The mascot existing in components/gomate-avatar.tsx is your starting point — extend it. Don't delete or break the existing avatar usage in the rest of the app.

Run pnpm tsc --noEmit. Render each component in isolation and verify it works.

When you're done, show me each component rendered with placeholder content. I want to see all 7 animation states.
```

---

## Prompt 2.6 — Build the new onboarding page

```
Build app/(app)/onboarding/page.tsx (or replace app/(app)/chat/page.tsx for collecting stage).

This page is the new mascot-based onboarding flow.

Layout:
- Center stage: the mascot
- Below mascot: speech bubble with current question
- Below bubble: onboarding input field
- Right side panel (collapsible on mobile): live profile preview that updates as fields fill in. Each field has audit icon — click opens audit-trail-popover (built later in Phase 4 — for now just placeholder).
- Top status: a soft phrase like "Building your profile" → "Almost ready" → "Profile complete" — controlled by Question Director's response. NO percentage counter. NO field count counter.

Behavior:
1. On mount, fetch user's profile via /api/profile.
2. If profile is empty: send a request to /api/chat with role=user, content="" (initial). Backend Question Director returns first question and animation cue (likely waving + name question). Render mascot wave, show question.
3. User types answer in input, hits submit.
4. Send POST to /api/chat with the new message. Show mascot in "thinking" state during the processing.
5. Receive response: { questionText, animationCue, isOnboardingComplete, updatedProfile }.
6. Update right-side panel with new profile state.
7. Update mascot animation per cue.
8. Show new question in speech bubble.
9. If isOnboardingComplete: mascot does celebrating animation. Show "Profile complete!" status. Show prominent button "Generate my plan" that calls POST /api/plans/trigger-research and transitions to research page.

Edge cases:
- User refreshes during onboarding: load existing partial profile, show mascot waving with "Welcome back, let's continue" or similar context-aware greeting.
- User types nonsense: Validator catches it, Question Director re-asks with retryHint.
- Slow agent processing (>3 seconds): keep mascot in "thinking" state, optionally show subtle progress indicators ("Analyzing your answer", "Preparing the next question").

Use existing Confetti component on celebration.

Use the existing useTier hook and existing layout.tsx wrapping. Don't break the existing app structure.

When you're done, walk me through the full onboarding flow end-to-end. Use the test input "I'm Axel, 30, moving to Sweden for work — my company is sending me on a 14-month posting with my wife and two kids." Show me how Question Director handles the bundled answer.
```

---

## Prompt 2.7 — Test the full onboarding pipeline

```
Test the multi-agent onboarding pipeline end-to-end. No new code unless tests reveal bugs.

Write a test scenario file at scripts/test-onboarding.mjs (or similar) that runs a sequence of mock onboarding turns and verifies behavior. Don't use a heavy test framework — just a direct script.

Scenario 1 — bundled answer:
Input message 1: "I'm Axel, 30 years old, moving to Sweden for work next year"
Expected: Extractor for pending=name returns "Axel" (explicit), additionalFieldsDetected=[birth_year, destination, purpose, target_date]. Validator passes. Profile Writer writes name=Axel. Question Director sees additionals → prioritizes confirming birth_year next ("Just to confirm — you're 30 years old, so born around 1995?").

Scenario 2 — yes/no normalization:
After above, input message 2: "Yeah that's right"
Expected: Extractor for pending=birth_year returns "yes" (because pending is yes-or-no in confirmation context — actually wait, birth_year is a number field). Let me reconsider.

Actually scenario 2 should be: Question Director phrased it as "you're 30, born in 1995, correct?" — so the pending field is indirectly a confirmation. Handle this by having Question Director return the next field as birth_year with a confirmation question. Extractor sees "Yeah that's right" against pending=birth_year — and infers from prior context that user is confirming 1995. Returns 1995 with confidence "inferred".

Scenario 3 — invalid answer:
Input message 3 (after asking destination): "I want to live somewhere warm"
Expected: Extractor returns "I couldn't tell" (no specific country). Question Director picks up the additionalFieldsDetected, asks clarification: "Got it, somewhere warm — any specific country in mind?"

Scenario 4 — posting detected:
Continue scenario 1. After establishing destination=Sweden and purpose=work, ask about job_offer. User: "Yes, my company is sending me — it's a 14-month posting." Expected: Extractor extracts job_offer=yes, posting_or_secondment=yes (additional), posting_duration_months=14 (additional). Question Director asks about home_country_employer next.

Scenario 5 — schema bounding:
Force Question Director to attempt asking about a non-schema field by manually setting profile to nearly-complete with all required filled but one. Check Question Director correctly picks the one remaining required field, never invents a non-schema topic.

Scenario 6 — completion:
Fill in all required fields. Profile is now complete per isProfileComplete. Send another user message ("Anything else?"). Question Director should return isOnboardingComplete=true with celebrating animation cue. Plan stage should transition to "complete".

Scenario 7 — heavy bundled extraction (the Spine-defining test):
Input message (single turn): "My name is Axel, I'm 30, moving from Germany to Sweden for a 14-month work posting with my wife and two kids."
Expected: Extractor (or chained Extractor calls if needed) populates: name=Axel, birth_year≈1995 (from age 30), origin=Germany, destination=Sweden, intent_type=work, posting_or_secondment=yes (from "work posting"), posting_duration_months=14, has_spouse=yes, spouse_joining=yes, has_children=yes, children_count=2. Validator passes all. Profile Writer writes all with audit trail showing the source phrase per field. Question Director then prioritizes the highest-leverage next missing required field (likely target_arrival_window or home_country_employer because posting was triggered).
This is the test that mirrors the Spine input. If this scenario fails, do NOT move on. The whole user journey collapses without bundled extraction.

Run all scenarios. Show me the output. Fix any failing scenario before moving to Phase 3.
```

---

# PHASE 3 — Multi-agent research

## Prompt 3.1 — Build the Coordinator with explicit dispatch rules

```
Build lib/agents/coordinator.ts.

The Coordinator is a head chef. It reads the user's complete profile and decides which specialist agents to dispatch. The decision is rule-based and explainable, not a black box. No LLM is needed for the dispatch decision itself — it's pure code. The Coordinator returns a list of specialists to run plus the rationale for each.

Function:
```
decideDispatch(profile: Profile): DispatchDecision

type DispatchDecision = {
  specialists: SpecialistInvocation[];
  rationale: { specialist: string, reason: string }[];
}

type SpecialistInvocation = {
  name: string;
  inputs: any; // profile slice the specialist needs
}
```

Always-run specialists:
- visa_specialist
- tax_strategist
- cost_specialist
- housing_specialist
- cultural_adapter
- documents_specialist
- healthcare_navigator (always but goes deeper if needs)
- banking_helper

Conditional specialists:
- schools_specialist if children_count > 0
- pet_specialist if pets ≠ "none"
- posted_worker_specialist if posting_or_secondment === "yes"
- digital_nomad_compliance if purpose === "digital_nomad"
- job_compliance_specialist if purpose === "work" AND job_offer === "yes" AND posting_or_secondment !== "yes"
- family_reunion_specialist if visa_role === "dependent" OR settlement_reason === "family_reunion"
- departure_tax_specialist if origin (current_location) is in EXIT_TAX_COUNTRIES list (Sweden, USA, Norway, Eritrea, France, Germany under some conditions, Netherlands)
- vehicle_import_specialist if bringing_vehicle === "yes"
- property_purchase_specialist if home_purchase_intent === "yes"
- trailing_spouse_career_specialist if spouse_joining === "yes" AND spouse_seeking_work === "yes"
- pension_continuity_specialist if pension_continuity_required === "yes"

For each dispatched specialist, the rationale is a one-line plain-English explanation: e.g., "Schools Specialist dispatched because user is moving with 2 children", "Posted Worker Specialist dispatched because EU-to-EU 14-month posting requires A1 certificate path".

Build EXIT_TAX_COUNTRIES as an exported constant in lib/gomate/exit-tax-list.ts: ["Sweden", "United States", "Norway", "Eritrea", "France", "Germany", "Netherlands", ...]. Reference: countries with exit tax or related provisions on emigration of citizens.

Add app/api/research/dispatch-preview/route.ts that returns the dispatch decision for the current user's profile without actually running anything. The visible agent panels page calls this first to render the panel grid before agents start.

Run pnpm tsc --noEmit.

When you're done, show me the dispatch decisions for these three profiles:
1. Filipino fiancée moving to Sweden as dependent, settling permanently with no kids/pets
2. German engineer posted to Stockholm for 18 months, family of 4 with 2 kids
3. American digital nomad to Spain solo with chronic condition and a dog
```

---

## Prompt 3.2 — Build the always-run specialists

```
Build the six always-run specialist agents in lib/agents/specialists/.

Each specialist follows the same pattern:
1. Read profile slice (just the fields needed).
2. Look up relevant URL from lib/gomate/official-sources.ts for destination country.
3. Scrape that URL via Firecrawl (use existing scrape pattern from lib/gomate/research-visa.ts).
4. Synthesize content for THIS user's specific situation using the model from router.ts.
5. Return structured output with citations.

Each specialist file: lib/agents/specialists/{name}.ts. Each exports a function:
```
async function run(profile: Profile, signal?: AbortSignal): Promise<SpecialistOutput>

type SpecialistOutput = {
  contentParagraphs: string[]; // each paragraph in our brand voice with [1] [2] inline citation markers
  citations: { number: number, sourceUrl: string, sourceName: string, retrievedAt: string }[];
  sourceUrlsUsed: string[];
  retrievedAt: string;
  quality: "full" | "partial" | "fallback";
  confidence: number; // 0-1
  domainSpecificData: any; // structured data per specialist (visa options, cost breakdown, etc.)
}
```

specialists/visa.ts:
- Reads citizenship, destination, purpose, visa_role, partner fields, prior_visa, prior_visa_country, prior_visa_type from profile.
- Scrapes official_sources[destination].immigration and official_sources[destination].visa.
- Produces visa pathway analysis with 3-8 visa options, each with eligibility/requirements/cost/processing time/benefits/limitations.
- Returns domainSpecificData with visaOptions array (compatible with existing VisaOption type).
- Use existing research-visa.ts logic as the implementation core; wrap it in the new specialist interface.

specialists/tax.ts:
- Reads citizenship, destination, current_location, purpose, posting_or_secondment, posting_duration_months, monthly_income, savings_available.
- Scrapes official_sources[destination].tax and official_sources[origin].tax (if origin has exit tax).
- Produces tax pathway analysis: residency rules, treaty references (for the specific origin-destination pair), exit tax obligations, entry tax structure, double-taxation treaty highlights.
- Returns domainSpecificData with taxResidencyRules, treatyReferences, exitTaxObligations, entryTaxStructure.
- Uses Opus 4.7 for treaty interpretation.

specialists/cost.ts:
- Reads destination, target_city, monthly_income, monthly_budget, savings_available, children_count, healthcare_needs.
- Uses existing cost-of-living-estimator.ts for cached or fresh data.
- Calls calculateMonthlyBudget() and calculateSavingsTarget() from existing web-research.ts.
- Returns domainSpecificData with monthlyBudget, savingsTarget, currencyConversion.

specialists/housing.ts:
- Reads destination, target_city, monthly_budget, rental_budget_max, furnished_preference, commute_tolerance_minutes, accessibility_needs, children_count.
- Scrapes official_sources[destination].housing.
- Produces neighborhood recommendations matched to commute tolerance and budget. Lists rental platforms. Notes deposit norms and contract red flags.
- Returns domainSpecificData with recommendedAreas, averageRents, rentalPlatforms, depositNorms.

specialists/cultural.ts:
- Reads citizenship, destination, target_city, purpose, family composition.
- No URL scraping — uses existing COUNTRY_DATA seed from lib/gomate/guide-generator.ts. Optionally scrapes a cultural-info URL if one exists.
- Produces country-pair-specific cultural notes — etiquette differences, working culture norms, dining norms, common gotchas a Filipino moving to Sweden would experience differently than a Brazilian.
- Use Sonnet 4.6 for warm tone.
- Returns domainSpecificData with etiquetteNotes, workingCulture, diningCulture, commonGotchas, languageTips.

specialists/documents.ts:
- Reads citizenship, destination, purpose, visa_role, marital_status implications, children_count, prior visa fields, posting fields.
- For each document required by the user's visa pathway (intersect with Visa Specialist's output), produces: document name, where to obtain in origin country with link, apostille requirement (yes/no, where to get apostille), translation requirement (yes/no, certified translator requirement), submission destination, processing time.
- For posting users, includes A1 certificate process (if EU) or CoC process (if non-EU treaty).
- Pre-fills draft URLs where the form is structured (Migrationsverket has online portal — link to it).
- Returns domainSpecificData with requiredDocuments array, apostilleChain, translationChain.

Each specialist also writes audit rows via writeAuditRow() at start (status="started"), at completion (status="completed" or "failed"), and for the synthesized output.

Use parallel execution with Promise.allSettled (existing pattern from research/trigger). Each specialist has its own AbortSignal for timeout (60 seconds default).

Run pnpm tsc --noEmit.

When you're done, run all six specialists for the German posting profile (citizenship=German, destination=Sweden, purpose=work, posting_or_secondment=yes, posting_duration_months=18, children_count=2). Show me the structured outputs and audit rows.
```

---

## Prompt 3.3 — Build the conditional specialists

```
Build the conditional specialists in lib/agents/specialists/.

specialists/schools.ts:
- Reads destination, target_city, children_count, children_ages, children_school_type_preference, children_language_skills_destination, children_special_needs.
- Scrapes destination-specific school directories if URLs are in official-sources. For Sweden, scrape Skolverket. For Germany, scrape KMK or specific Bundesland. For Spain, scrape Ministerio de Educación.
- For each child by age, recommends 3-5 schools matching age, language ability, and preference type. Lists application timelines (international schools have very long lead times — many require applications 6-12 months in advance), application fees, language of instruction, and waitlist info.
- Returns domainSpecificData with childRecommendations (one per child) array.
- Uses Gemini 2.5 for breadth.

specialists/healthcare.ts:
- Always runs. Goes deeper if healthcare_needs ≠ "none" or chronic_condition_description exists.
- Reads destination, target_city, healthcare_needs, chronic_condition_description, prescription_medications, english_speaking_doctor_required, children_count.
- Scrapes destination's healthcare authority URL.
- Always includes: registration steps for primary care, insurance options, finding family doctor.
- For chronic conditions: includes specialist availability, prescription medication availability (some are restricted), insurance coverage for ongoing treatment.
- For users wanting English-speaking doctors: lists known English-friendly medical centers.
- Returns domainSpecificData with registrationSteps, insuranceOptions, recommendedProviders, prescriptionContinuity.

specialists/banking.ts:
- Always runs. Goes deeper if need_budget_help=yes.
- Reads destination, target_city, citizenship, purpose, posting_or_secondment, monthly_income, savings_available.
- Scrapes destination's central bank or banking authority URL.
- Lists 3-5 banks foreigners can open accounts at, with required documents per bank, typical processing time, English service availability, fee structure.
- Always includes Wise/Revolut/N26 as bridge accounts before local account opens.
- Returns domainSpecificData with recommendedBanks, requiredDocs, digitalBridges, accountOpeningSteps.

specialists/pet.ts:
- Conditional on pets ≠ "none".
- Reads destination, pet_microchip_status, pet_vaccination_status, pet_size_weight, pet_breed, pet_age.
- Scrapes destination's veterinary import authority URL (e.g., Sweden's Jordbruksverket).
- Returns: import requirements, microchip and vaccination timeline (rabies has 21-day post-vaccination wait for EU travel), banned breed list (some destinations restrict bull terriers, certain mastiff breeds), import permit application process, quarantine rules if applicable.
- Returns domainSpecificData with importRequirements, vaccinationTimeline, breedRestrictions, importPermit, quarantineRules.

specialists/posted_worker.ts:
- Conditional on posting_or_secondment === "yes".
- Reads citizenship, destination, current_location, posting_duration_months, home_country_employer, posting_employer_address.
- Scrapes:
  - For EU-to-EU: destination's labor authority for Posted Worker Declaration rules (e.g., Sweden's Arbetsmiljöverket Posting of Workers register, Germany's ZOLL).
  - Origin country's social security agency for A1 certificate process.
  - For non-EU bilateral treaties (US-Sweden, India-Sweden, etc.): the relevant treaty's CoC application process.
- Returns: A1 certificate application steps (or CoC for non-EU), Posted Worker Declaration filing steps with destination's labor authority, employer registration requirement (must designate contact person in destination), social security continuity rules (max 24 months for EU postings).
- Returns domainSpecificData with a1OrCocPath, pwdFilingProcess, employerRegistration, contactPersonRequirement, socialSecurityRules.

specialists/digital_nomad_compliance.ts:
- Conditional on purpose === "digital_nomad".
- Reads citizenship, destination, monthly_income, income_consistency, income_history_months, remote_income.
- Scrapes destination's digital nomad visa requirements URL.
- Returns: income threshold for the visa (compared to user's actual), tax residency implications, registration requirements, validity period.
- Returns domainSpecificData with incomeThreshold, incomeQualifies (boolean), taxResidencyImplications, visaValidity.

specialists/job_compliance.ts:
- Conditional on purpose === "work" AND job_offer === "yes" AND posting_or_secondment !== "yes".
- Reads citizenship, destination, employer_sponsorship, highly_skilled, education_level, years_experience.
- Scrapes destination's work permit authority URL.
- Returns: work permit requirements specific to user's situation, employer sponsorship process, salary thresholds for EU Blue Card or similar, processing timelines.
- Returns domainSpecificData with permitRequirements, sponsorshipProcess, salaryThreshold.

specialists/family_reunion.ts:
- Conditional on visa_role === "dependent" OR settlement_reason === "family_reunion".
- Reads citizenship, destination, partner_citizenship, partner_visa_status, relationship_type, partner_residency_duration, relationship_duration.
- Scrapes destination's family reunion visa requirements URL.
- Returns: family reunion visa requirements (income thresholds for sponsor, accommodation requirements, integration test if applicable), processing time, how dependent's residency derives from primary's status.
- Returns domainSpecificData with familyReunionRequirements, sponsorIncomeThreshold, derivedResidencyRules.

specialists/departure_tax.ts:
- Conditional on origin in EXIT_TAX_COUNTRIES.
- Reads citizenship, current_location, destination, savings_available, monthly_income, pension_continuity_required.
- Scrapes origin country's tax authority for exit tax / departure declaration rules.
- Returns: exit tax obligations specific to user's situation, capital gains implications, pension and retirement account treatment, when to file departure declaration, treaties that may reduce double-taxation.
- Uses Opus 4.7.

specialists/vehicle_import.ts:
- Conditional on bringing_vehicle === "yes".
- Reads destination, vehicle_make_model_year, vehicle_origin_country, vehicle_emission_standard.
- Scrapes destination's customs and vehicle registration authority URLs.
- Returns: import duty calculation, emissions compliance check (EU import rules: must meet Euro 6 for many countries), customs declaration process, registration steps after import, license plate process, technical inspection requirements.
- Returns domainSpecificData with importDuty, emissionsCompliance, customsProcess, registrationSteps.

specialists/property_purchase.ts:
- Conditional on home_purchase_intent === "yes".
- Reads destination, target_city, monthly_budget, savings_available, citizenship.
- Scrapes destination's real estate authority and tax info.
- Returns: foreign buyer restrictions (some countries restrict foreign property purchase), mortgage availability for non-residents, transaction taxes (stamp duty, transfer tax), recommended real estate agents, typical purchase process timeline.

specialists/trailing_spouse_career.ts:
- Conditional on spouse_joining === "yes" AND spouse_seeking_work === "yes".
- Reads destination, spouse_career_field, spouse_language_skills.
- Scrapes general labor market info for destination + specific industry resources.
- Returns: labor market overview for spouse's field, networking platforms, professional associations, language requirements per industry, work permit considerations for spouse (does dependent visa allow employment).
- Uses Sonnet 4.6 for warmer tone (this is emotionally significant — career disruption).

specialists/pension_continuity.ts:
- Conditional on pension_continuity_required === "yes".
- Reads citizenship, current_location, destination, employer details if posting.
- Scrapes pension authorities in both countries.
- Returns: pension fund continuity rules, treaty implications, when contributions transfer vs. accumulate separately, retirement age implications, withdrawal rules from origin pension upon emigration.
- Uses Opus 4.7.

Run pnpm tsc --noEmit.

When you're done, run the relevant conditional specialists for the three profiles from Prompt 3.1. Show me the outputs and confirm the right specialists fired.
```

---

## Prompt 3.4 — Build the Synthesizer and Critic

```
Build lib/agents/synthesizer.ts and lib/agents/critic.ts.

Synthesizer (uses Opus 4.7):
After all specialists finish, the Synthesizer reads all their outputs and ensures consistency across them.

Function:
```
synthesize(specialistOutputs: { name: string, output: SpecialistOutput }[]): Promise<UnifiedGuide>

type UnifiedGuide = {
  sections: { key: string, title: string, paragraphs: string[], citations: Citation[] }[];
  consistencyIssues: string[]; // anything the Synthesizer noticed and resolved
  unresolvedIssues: string[]; // anything that needs human review
}
```

The Synthesizer's checks:
- Deadlines align: Visa Specialist says processing takes 8 weeks, Pre-departure timeline (built later) should reflect that.
- Costs add up: Cost Specialist's monthly budget should align with Tax Strategist's net income calculations.
- Document requirements don't contradict between specialists: Visa Specialist might list "police clearance from origin"; Documents Specialist must produce the apostille chain for that exact document.
- Vocabulary consistency: always "personnummer" not sometimes "Swedish ID number".
- No duplicated content across sections.
- All claims have citations attached.

If the Synthesizer detects unresolvable conflicts, it surfaces them in unresolvedIssues — the Critic and ultimately the user need to know.

Critic (uses Sonnet 4.5 with adversarial prompt):
The Critic reads the unified guide and the user's profile. Its system prompt is adversarial: "You are looking for what's missing or weak. Your job is not to validate. Your job is to find the gap. Be ruthless."

Function:
```
critique(profile: Profile, guide: UnifiedGuide): Promise<CriticOutput>

type CriticOutput = {
  gaps: { area: string, description: string, suggestedSpecialist?: string }[];
  weakClaims: { claim: string, location: string, reason: string }[];
  missingForUserSituation: string[]; // things specific to this profile that should be addressed but aren't
}
```

Examples of what Critic should catch:
- User has chronic_condition_description="Type 1 diabetes" but Healthcare Navigator output didn't address insulin availability or prescription continuity in destination.
- User has posting_duration_months=24 (right at the EU A1 limit) but Posted Worker Specialist didn't address what happens at month 24 (extension procedure, social security transition).
- Visa Specialist recommended a pathway that requires a clean criminal record but Documents Specialist didn't include police clearance in the document chain.
- User mentioned bringing a Staffordshire Bull Terrier but Pet Specialist didn't note that some Swedish municipalities have breed restrictions on bull terrier types.

If gaps are found, the Coordinator (next prompt) decides whether to dispatch additional specialist runs or surface to the user.

Both agents write audit rows on completion.

Run pnpm tsc --noEmit.

When you're done, run Synthesizer + Critic on the German posting profile's specialist outputs. Show me what the Critic finds.
```

---

## Prompt 3.5 — Build the research orchestration route + visible agent panels

```
Now wire up the research orchestration to actually run, and build the UI for visible agent panels.

Step 1: Replace app/api/research/trigger/route.ts.

The new logic:
1. Auth check.
2. Verify the user clicked "Generate my plan" (user_triggered_research_at is set).
3. Verify plan stage is "complete" or transitioning.
4. Call Coordinator.decideDispatch(profile) to get the specialist list.
5. Update relocation_plans.research_status = "in_progress".
6. Run all specialists in parallel via Promise.allSettled, each wrapped with the orchestrator's retry/audit.
7. After all specialists finish, run Synthesizer.
8. Run Critic on synthesized guide.
9. If Critic finds gaps with suggestedSpecialist, optionally dispatch one more round (cap at 1 to prevent infinite loops).
10. Set relocation_plans.research_status = "completed" or "partial" (if some specialists failed).
11. Set research_completed_at = now().
12. Set stage = "ready_for_pre_departure".

Step 2: Build the agent panel UI at app/(app)/research/page.tsx.

Layout:
- Top: Coordinator panel showing rationale ("Visa Specialist dispatched because...", "Posted Worker Specialist dispatched because...").
- Main grid: one panel per dispatched specialist. Each panel shows:
  - Agent name and emoji icon
  - Status: idle (not started yet) | researching (scraping) | drafting (synthesizing) | validating (cross-checking) | complete | failed
  - Current activity in plain English: "Reading Migrationsverket residence permit page", "Computing exit tax for Sweden departure"
  - Source URLs being consulted (with live links, small icons showing scrape status)
  - Progress (e.g., "3 of 5 sources read")
  - When complete: brief 2-line summary of what was found
  - Click to expand: full draft section the agent produced

- Bottom: when Synthesizer is running, panels start to fade. Unified guide starts rendering inline.
- When Critic completes: any new specialist dispatches appear as new panels.

Step 3: Build agent-panel.tsx and agent-panel-grid.tsx components.

Step 4: Use Server-Sent Events (SSE) to push agent status updates from backend to UI. Add app/api/agents/[name]/status/route.ts that streams events: { agentName, status, currentActivity, sourceUrls, progress }. The frontend subscribes and updates panels in real-time.

Step 5: When user navigates to /research while research is running, they see panels. When research completes, page transitions to /dashboard with the new guide ready.

Important — DO NOT auto-trigger research on profile completion. Research only starts when user_triggered_research_at is set, which only happens via the explicit POST /api/plans/trigger-research endpoint.

Run pnpm tsc --noEmit.

When you're done, walk me through running research for the German posting profile. Show me the panels populating live.
```

---

## Prompt 3.6 — Test research orchestration with multiple profiles

```
Manual testing only — no new code unless bugs found.

Run the full research orchestration for these three profiles in your local Replit dev environment:

Profile A — Filipino fiancée moving to Sweden:
- citizenship=Filipino
- current_location=Philippines
- destination=Sweden
- target_city=Stockholm
- purpose=settle
- visa_role=dependent
- partner_citizenship=Swedish
- partner_visa_status=citizen
- relationship_type=fiancé
- moving_alone=yes (Roselle moves first, Axel is already in Sweden)
- pets=none
- children_count=0
- healthcare_needs=none

Expected specialists dispatched: visa, tax, cost, housing, cultural, documents, healthcare, banking, family_reunion. NOT dispatched: schools, pet, posted_worker, digital_nomad_compliance, job_compliance, departure_tax (Philippines has no exit tax for emigrants, depending on definition), vehicle_import, property_purchase, trailing_spouse_career, pension_continuity.

Profile B — German engineer posted to Stockholm for 18 months:
- citizenship=German
- current_location=Germany
- destination=Sweden
- target_city=Stockholm
- purpose=work
- visa_role=primary
- moving_alone=no
- spouse_joining=yes
- spouse_career_field=marketing
- spouse_seeking_work=yes
- spouse_language_skills=medium
- children_count=2
- children_ages="8 and 5"
- children_school_type_preference=international
- pets=none
- job_offer=yes
- posting_or_secondment=yes
- posting_duration_months=18
- home_country_employer="Berlin GmbH"

Expected specialists dispatched: visa, tax, cost, housing, cultural, documents, healthcare, banking, schools, posted_worker, family/trailing_spouse, departure_tax (Germany has exit tax under some conditions). NOT dispatched: pet, digital_nomad, vehicle_import (assume not bringing).

Profile C — American digital nomad to Spain solo with chronic condition and dog:
- citizenship=American
- current_location=USA
- destination=Spain
- target_city=Barcelona
- purpose=digital_nomad
- moving_alone=yes
- monthly_income="6000 USD"
- income_consistency=stable
- income_history_months=24
- remote_income=yes
- pets=dog
- pet_breed=Golden Retriever
- pet_age="5 years"
- pet_microchip_status=yes
- pet_vaccination_status=current
- healthcare_needs=chronic_condition
- chronic_condition_description="Type 1 diabetes, daily insulin"
- prescription_medications=yes
- english_speaking_doctor_required=yes

Expected specialists: visa, tax, cost, housing, cultural, documents, healthcare (deep), banking, pet, digital_nomad_compliance, departure_tax (USA has citizenship-based taxation — extensive exit considerations).

For each profile:
1. Trigger research via /api/plans/trigger-research (set user_triggered_research_at).
2. Watch the visible agent panels.
3. After completion, verify the unified guide has sections from all dispatched specialists.
4. Verify Critic's output flagged anything specific (e.g., for Profile C: insulin import rules, EU regulations on medical supplies).

Show me the panels for each profile. Show me the Critic's output for each. Fix any specialist that didn't fire when it should have, or vice versa.
```

---

# PHASE 4 — Profile-aware dynamic dashboard

## Prompt 4.1 — Build profile-aware dashboard layout

```
Transform app/(app)/dashboard/page.tsx into profile-aware dynamic rendering.

Currently it renders a hardcoded set of cards. Replace with a layout that reads profile and dispatches cards per shouldShow predicates.

Step 1: Update lib/gomate/dashboard-state.ts. Add a new function:
```
computeVisibleCards(profile: Profile, plan: PlanStateInput): VisibleCardSet

type VisibleCardSet = {
  cards: { id: string, priority: number }[];
  alwaysShow: string[];
  conditionallyShown: { id: string, reason: string }[];
}
```

Mapping (already-exist or new card IDs):
- visa-routes-card: always
- cost-of-living-card: always
- affordability-card: always
- profile-details-card: always
- visa-research-card: always after research complete
- visa-status-stepper: after visa pathway selected (visa_application.selectedVisaType set)
- visa-deadline-card: after research complete and pathway selected
- document-progress-card: after research complete
- interactive-document-checklist: after research complete
- banking-wizard-card: after research complete
- tax-overview-card: always (deeper if departure_tax_filing_required=yes)
- cultural-card: always
- pre-departure-timeline: when stage is pre_departure
- compliance-alerts: only when arrived
- compliance-calendar: only when arrived
- compliance-timeline: only when arrived
- settling-in-task-card: only when arrived
- wellbeing-checkin: only when arrived
- arrival-banner: when stage transitions to arrived (first 30 days)
- plan-consistency-alerts: always
- commonly-forgotten-section: always
- schools-card: when children_count > 0
- pet-relocation-card: when pets ≠ "none"
- income-compliance-card: when purpose === "digital_nomad"
- family-reunion-card: when visa_role === "dependent" OR settlement_reason === "family_reunion"
- departure-tax-card: when current_location is in EXIT_TAX_COUNTRIES
- vehicle-import-card: when bringing_vehicle === "yes"
- property-purchase-card: when home_purchase_intent === "yes"
- posted-worker-card: when posting_or_secondment === "yes"
- trailing-spouse-card: when spouse_joining === "yes" AND spouse_seeking_work === "yes"
- chronic-health-card: when healthcare_needs ≠ "none" OR chronic_condition_description exists
- prior-visa-history-card: when prior_visa === "yes" OR visa_rejections === "yes"

Step 2: Refactor app/(app)/dashboard/page.tsx to call computeVisibleCards and render only the returned cards. Layout: responsive grid, cards reflow based on count. Card priority order: visa first (most critical), then deadline-sensitive (departure_tax, schools application timing, posted_worker registration), then financial, then advisory.

Step 3: Build the new conditional card components in the next prompt. For now, use placeholder components that render "[Card name] — coming next prompt".

Existing dashboard cards keep working. Don't touch their internal logic.

Run pnpm tsc --noEmit.

When you're done, walk me through the dashboard for three profiles: solo Filipino dependent (should hide schools, pet, posted-worker, vehicle, property), German posting family (should show schools, posted-worker, departure-tax), American DN with dog (should show pet, income-compliance, chronic-health, departure-tax).
```

---

## Prompt 4.2 — Build the new conditional dashboard cards

```
Build the new conditional cards as components.

components/schools-card.tsx:
Renders Schools Specialist's domainSpecificData.childRecommendations. For each child, shows: 3-5 recommended schools, application timeline urgency, application fees, language of instruction, waitlist status. Inline citation markers linking to source URLs.

components/pet-relocation-card.tsx:
Renders Pet Specialist output. Shows: import requirements, vaccination timeline (with countdown to EU travel readiness if applicable), breed restrictions if breed is on a restricted list, import permit application steps, quarantine rules.

components/income-compliance-card.tsx:
Renders Digital Nomad Compliance Specialist output. Shows: required income threshold for the destination's nomad visa, comparison to user's actual income (qualifies / doesn't), tax residency implications, registration requirements, validity period.

components/family-reunion-card.tsx:
Renders Family Reunion Specialist output. Shows: family reunion visa requirements, sponsor income threshold, accommodation requirements, integration test if applicable, processing time, derived residency rules (how user's permission depends on sponsor's status).

components/departure-tax-card.tsx:
Renders Departure Tax Specialist output. Shows: exit tax obligations, capital gains implications, pension treatment, departure declaration deadline, treaty references for double-taxation relief.

components/vehicle-import-card.tsx:
Renders Vehicle Import Specialist output. Shows: import duty, emissions compliance check, customs declaration steps, registration steps, license plate process, technical inspection schedule.

components/property-purchase-card.tsx:
Renders Property Purchase Specialist output. Shows: foreign buyer restrictions, mortgage availability, transaction taxes, agent recommendations, purchase timeline.

components/posted-worker-card.tsx:
Renders Posted Worker Specialist output. Shows:
- A1 certificate status (or CoC status for non-EU): checklist of steps, where to apply (origin's social security agency), expected timeline.
- Posted Worker Declaration status: filed yes/no, filing deadline (must be before work starts), filing portal URL.
- Employer contact person registration status.
- Social security continuity rules.
This card shows the most prominent status indicators because posting users have legally critical compliance.

components/trailing-spouse-card.tsx:
Renders Trailing Spouse Career Specialist output. Shows: labor market overview for spouse's field, networking platforms, professional associations, language requirements per industry, work permit considerations for spouse.

components/chronic-health-card.tsx:
Renders deep-dive Healthcare output specific to chronic condition. Shows: specialist availability in destination, prescription medication availability (some are restricted), insurance coverage for ongoing treatment, English-speaking doctor recommendations.

components/prior-visa-history-card.tsx:
Renders prior immigration history context. Shows: warning if visa_rejections=yes (need to address in new application), list of prior visa countries and types. This is preparatory — the user doesn't take action here, but it's surfaced for transparency.

All cards follow the same pattern:
- Use shadcn Card component.
- Title + status badge.
- Content with inline citation markers (see Phase 6 for citation rendering).
- Audit icons next to every value (see next prompt for audit-trail-popover).
- "View source" links to expand and see scraped content excerpt.

Run pnpm tsc --noEmit.

When you're done, render each new card with sample data and screenshot.
```

---

## Prompt 4.3 — Build the audit trail popover system

```
Build the audit trail popover system that overlays all dashboard cards.

components/audit-icon.tsx:
A small information icon (info-circle from lucide-react). Placed next to any displayed value on the dashboard. Click triggers the popover.

Props:
```
type AuditIconProps = {
  fieldKey?: AllFieldKey; // for profile fields
  outputKey?: string; // for research outputs (sectionKey.paragraphIdx or similar)
  size?: "xs" | "sm" | "md";
}
```

components/audit-trail-popover.tsx:
The popover that opens when audit icon is clicked. Use shadcn Popover.

For profile fields:
- Title: "How we got this"
- "Extracted by [agent name]" with model used
- Confidence: explicit (high) | inferred (medium) | assumed (low) — displayed with color-coded badge
- "From your message: [excerpt]" — show 200 chars of the relevant user message
- "Validated: [rules applied]"
- Timestamp
- Edit button: lets user correct the value if wrong

For research outputs:
- Title: "Source for this claim"
- "[Specialist name] researched this"
- Source URL with "Open" button (opens in new tab)
- "Retrieved: [timestamp] ([N] days ago)" — colored if older than 7 days
- "Quality: [full | partial | fallback]" — colored badge
- Specialist's confidence
- "Verify on official site" button

For derived values (computed budgets, etc.):
- Title: "How this was calculated"
- Inputs used with their audit links
- Formula applied
- Last computed at

Add app/api/agent-audit/route.ts that returns the audit data:
- GET ?profile_id=X&field_key=Y → returns latest audit row for that field
- GET ?guide_id=X&section_key=Y&paragraph_idx=Z → returns citation row

The endpoint queries agent_audit and guide_section_citations tables.

Migrate existing dashboard cards (visa-routes-card, cost-of-living-card, affordability-card, profile-details-card, etc.) to add audit-icon next to each displayed value. Don't rewrite the cards — just inject the audit-icon component next to values.

Run pnpm tsc --noEmit.

When you're done, walk me through clicking audit icons on three different dashboard fields and three different research outputs. Show me each popover.
```

---

# PHASE 5 — Pre-departure phase (NEW)

## Prompt 5.1 — Build the pre-departure timeline computation library

```
Build lib/gomate/pre-departure.ts.

This module is responsible for computing the pre-departure timeline based on user profile and visa pathway. It does NOT call agents directly — it's a pure function that gathers contributions from multiple specialist agents and orders them into a timeline.

Function signature:
```
async function generatePreDepartureTimeline(
  profile: Profile,
  selectedVisaPathway: VisaOption,
  moveDate: Date,
  specialistOutputs: { name: string, output: SpecialistOutput }[]
): Promise<PreDepartureTimeline>

type PreDepartureTimeline = {
  actions: PreDepartureAction[];
  totalActions: number;
  longestLeadTimeWeeks: number;
  criticalPath: PreDepartureAction[]; // actions on the critical path (delay these = delay arrival)
}

type PreDepartureAction = {
  id: string;
  title: string;
  description: string;
  weeksBeforeMoveStart: number;
  weeksBeforeMoveDeadline: number;
  estimatedDurationDays: number;
  dependsOn: string[]; // other action IDs
  documentsNeeded: string[];
  officialSourceUrl: string | null;
  preFilledFormUrl: string | null;
  agentWhoAddedIt: string;
  legalConsequenceIfMissed: string;
  status: "not_started";
  sortOrder: number;
}
```

Each specialist that has time-sensitive pre-departure actions contributes:

Visa Specialist contributes (if visa needed):
- Visa application starts (typically 8-12 weeks before)
- Required document gathering (varies by pathway)
- Visa application submission (4-6 weeks before)
- Embassy/consulate appointment if required
- Visa pickup before departure

Documents Specialist contributes:
- Birth certificate apostille (6-8 weeks lead time in some countries — applied for at start of timeline)
- Marriage certificate apostille (if applicable)
- Diploma apostille (if visa requires)
- Police clearance application (4-6 weeks)
- Translation chains for each apostilled document
- Pre-filled draft forms downloads

Schools Specialist contributes (if children):
- School applications begin (some international schools require 6-12 months lead time)
- Application document submission deadlines
- Acceptance confirmation milestones
- Final placement confirmation

Tax Strategist contributes (if departure tax applies):
- Departure declaration filing (typically end of tax year before move)
- Capital gains realization decisions
- Pension fund decisions

Banking Helper contributes:
- Bridge account setup (Wise/Revolut) — start now
- Notify origin bank of move
- Set up international transfer capability

Pet Specialist contributes (if pets):
- Microchip verification (if not already done)
- Rabies vaccine timing (21-day post-vaccination wait for EU travel)
- EU pet passport application (if EU)
- Pre-flight vet check
- Animal export permit if required

Posted Worker Specialist contributes (if posting):
- A1 certificate application (origin social security agency, 4-6 weeks)
- Posted Worker Declaration filing (must be before work starts, typically 2-4 weeks)
- Employer contact person designation
- Insurance arrangement for posting period

Healthcare Navigator contributes:
- Health insurance for travel period
- Medication supply (3-month supply if possible — some medications hard to get in destination)
- Medical record transfer requests

Vehicle Import Specialist contributes (if bringing vehicle):
- Customs declaration prep
- Emissions compliance check (technical inspection)
- Shipping or driving arrangement
- License plate cancellation in origin

Departure Tax Specialist contributes (if applicable):
- Final tax year filing
- Pension and retirement account decisions

Always-applicable timeline items (added by a generic "Departure Coordinator" sub-routine):
- 12 weeks before: Initial visa eligibility check (already done in research, but listed as confirmation)
- 8-10 weeks before: Major lead-time items (schools, apostille, A1)
- 4-6 weeks before: Visa submission, banking pre-setup, health insurance
- 2-4 weeks before: Address registration changes in origin (Abmeldung in Germany), mail forwarding, lease termination
- 1-2 weeks before: Final documents check, transit logistics, day-1-priorities printed
- Day of move: Travel checklist, what's in carry-on (passports, visa stickers, A1 certificates, all apostilled originals)

For each action:
- Compute weeksBeforeMoveStart from moveDate.
- Determine dependencies: e.g., visa submission depends on apostille completion + police clearance; A1 certificate depends on employer designation; school placement confirmation depends on application submission.
- Sort by weeksBeforeMoveStart descending (earliest first).
- Compute critical path using topological sort of dependencies + earliest start times.

Return the timeline. Don't write to database here — that happens in the next prompt.

Run pnpm tsc --noEmit.

When you're done, generate the pre-departure timeline for the German posting profile (move date 6 months from now) and the Filipino dependent profile (move date 4 months from now). Show me both timelines.
```

---

## Prompt 5.2 — Build pre-departure data API and database operations

```
Wire up the pre-departure timeline to the database and add the API for querying/updating it.

Step 1: Build app/api/pre-departure/route.ts.

GET /api/pre-departure → returns the user's current pre-departure timeline (or 404 if not generated yet).
POST /api/pre-departure/generate → generates the timeline (only callable when stage="ready_for_pre_departure" and user_triggered_pre_departure_at is null).
PATCH /api/pre-departure/[actionId] → updates an action's status (in_progress / complete / blocked / skipped).

POST /api/pre-departure/generate logic:
1. Auth check.
2. Verify plan stage is ready_for_pre_departure.
3. Verify user_triggered_pre_departure_at is null (don't regenerate).
4. Set user_triggered_pre_departure_at = now().
5. Get specialistOutputs from agent_audit table for this profile (filter to research-phase outputs).
6. Get the user's selected visa pathway (relocation_plans.visa_application.selectedVisaType joined with the visa research data).
7. Get the user's move date (relocation_plans.target_date or arrival_date pre-move).
8. Call generatePreDepartureTimeline.
9. Save all returned PreDepartureAction items to pre_departure_actions table.
10. Set stage = "pre_departure".
11. Return the timeline.

Add a new sub-coordinator agent if needed: lib/agents/pre_departure_coordinator.ts. This agent is OPTIONAL — its job is to take all the specialist contributions and produce a coherent narrative for the timeline (e.g., grouping related actions, generating descriptions for actions that span multiple specialists). Most of the logic can be in pure code in pre-departure.ts; only the narrative wrapping needs an LLM call. Use Opus 4.7 for this.

The narrative wrapper:
- For each action group (e.g., "Document apostille chain"), it produces a one-paragraph description of what's happening and why it matters.
- It identifies cross-cutting concerns (e.g., "Documents need apostille → translation → submission" — show this as a chain).
- It surfaces the most-urgent actions to the top of each weekly bucket.

Add database persistence helpers: getPreDepartureActions(profile_id) returns the user's actions sorted by weeksBeforeMoveStart, updatePreDepartureActionStatus(actionId, status, completedAt?) updates one.

Run pnpm tsc --noEmit.

When you're done, generate and save a pre-departure timeline. Show me the rows in pre_departure_actions table.
```

---

## Prompt 5.3 — Build the pre-departure UI

```
Build the pre-departure timeline UI.

components/pre-departure-timeline.tsx:
Extends the existing pre-move-timeline.tsx. The new component renders the full pre-departure phase.

Layout:
- Top: header with move date countdown ("Move in 12 weeks 3 days") and status overview ("18 actions, 4 completed, 2 in progress, 12 not started").
- Main: a vertical timeline organized by weeks-before-move, with sections for each weekly bucket (12 weeks before, 10 weeks before, 8 weeks before, etc.).
- For each bucket: list of actions due to start that week, with cards showing: title, weeks remaining until deadline, dependencies (with visual indicators if any are not yet complete), source link, status checkbox.
- Critical path is highlighted: actions whose delay would push back the move date have a colored border.
- Click an action to expand: full description, documents needed, official source URL, pre-filled form download (if applicable), legal consequence if missed, timestamp of last status update.
- Status update: click checkbox or buttons (Not Started → In Progress → Complete). Calls PATCH /api/pre-departure/[actionId].
- For dependencies: clicking on a dependency badge scrolls to that action.

Below the timeline:
- "Sources" section listing all official source URLs used across actions, deduplicated.
- "Stuck?" section linking to the post-arrival chat (which we'll polish in Phase 8) where user can ask for clarification on any action.

Add the page at app/(app)/pre-departure/page.tsx (or render the timeline inline on the dashboard when stage=pre_departure — your call, do whichever fits the existing layout better).

Update app/(app)/dashboard/page.tsx to include pre-departure-timeline component when stage="pre_departure".

Add the "Generate my pre-departure checklist" button to the dashboard when stage="ready_for_pre_departure". Click triggers POST /api/pre-departure/generate and transitions to /pre-departure (or refreshes dashboard with timeline rendered).

Run pnpm tsc --noEmit.

When you're done, walk me through the full pre-departure UI for the German posting profile. I want to see the A1 certificate action with proper deadline, apostille chain dependencies, and the critical path highlighted.
```

---

## Prompt 5.4 — Wire up the manual generation triggers

```
Wire up the two manual generation triggers in the UI.

Trigger 1: "Generate my plan" button.
- Appears on the dashboard when stage="complete" and user_triggered_research_at is null.
- Click → POST /api/plans/trigger-research.
- Backend sets user_triggered_research_at = now() and starts research orchestration.
- UI transitions to /research page where visible agent panels show progress.

Trigger 2: "Generate my pre-departure checklist" button.
- Appears on the dashboard when stage="ready_for_pre_departure" and user_triggered_pre_departure_at is null.
- Click → POST /api/pre-departure/generate.
- Backend generates timeline and transitions to /pre-departure stage.

Both buttons should be:
- Visually prominent (large, primary color, clear call to action).
- Have a brief explanation below ("This will analyze your profile and produce your personalized relocation plan — takes about 90 seconds." / "This will produce your week-by-week pre-departure checklist — takes about 30 seconds.").
- Show loading state while triggering.
- Handle errors gracefully (e.g., if research is already in progress, show "Already generating, please wait...").

Add the "I have arrived" trigger as well — already exists in v1 at /api/settling-in/arrive. Make sure it's still prominent on the dashboard during pre_departure stage approaching/past move_date.

Update the existing arrival-banner.tsx if needed to reflect the new lifecycle stages.

Confirm the lifecycle works end-to-end:
1. Profile complete → user sees "Generate my plan" button → clicks → research runs → guide rendered → stage=ready_for_pre_departure.
2. User sees "Generate my pre-departure checklist" button → clicks → timeline generated → stage=pre_departure.
3. User does pre-departure work over weeks, marking actions complete.
4. Move date approaches. User clicks "I have arrived" → settling-in DAG generated (Phase 7) → stage=arrived.

When you're done, walk me through the full lifecycle for one profile. I want to see all three manual trigger button presses and the state transitions they cause.
```

---

# PHASE 6 — Multi-agent guide enrichment + citations

## Prompt 6.1 — Build Guide Composer + section writers

```
Replace lib/gomate/guide-enrichment.ts with multi-agent guide composition.

Build lib/agents/guide_composer.ts and one section writer per guide section in lib/agents/section_writers/.

Section writers (use Sonnet 4.6 for warm brand voice):
- visa.ts — visa terminology, structures pathway comparisons, step-by-step application instructions, documentation requirements
- budget.ts — cost-of-living analysis, monthly budget breakdown, savings target methodology, salary requirements
- housing.ts — neighborhood comparison, rental market overview, contract review checklist, deposit norms
- banking.ts — account requirements per nationality, foreigner-friendly banks comparison, BankID equivalent setup
- healthcare.ts — registration steps, insurance options, finding English-speaking doctors, prescription continuity
- culture.ts — etiquette, social norms, working culture, dining culture, country-pair-specific gotchas
- jobs.ts — only if purpose=work — labor market overview, networking platforms, salary norms, employment contract norms
- education.ts — only if purpose=study OR children_count > 0 — admission processes, school types, language considerations
- documents.ts — required documents per visa pathway, apostille chains, translation requirements, submission portals
- posted_worker.ts — only if posting_or_secondment=yes — A1 certificate process, PWD filing, employer registration, social security continuity rules
- pre_departure_overview.ts — overview narrative for the pre-departure timeline (the timeline data is already generated in Phase 5; this is the prose explaining it)
- settling_in_overview.ts — overview narrative for post-arrival tasks

Each writer takes:
```
async function write(profile: Profile, specialistOutput: SpecialistOutput): Promise<SectionContent>

type SectionContent = {
  paragraphs: string[]; // each paragraph in our brand voice with [1] [2] inline citation markers
  citations: { number: number, sourceUrl: string, sourceName: string, retrievedAt: string }[];
}
```

Each writer's prompt is focused on its domain only. It reads the specialist's research output as input and writes prose in our brand voice (warm, confident, citation-heavy). Inline citation markers like [1] [2] [3] reference the citations array.

Guide Composer (uses Opus 4.7):
After all section writers finish, the Composer reads the full guide and runs a consistency pass:
- Same vocabulary for technical terms (e.g., always "personnummer" not sometimes "Swedish ID number")
- No contradictions between sections (deadlines align between visa/posted-worker/pre-departure)
- No duplicate information
- Tone consistency
- Citation numbering is unique across the guide (recompute citation numbers across sections so [1] is unique guide-wide, not per-section)

Composer output:
```
type ComposedGuide = {
  sections: {
    key: string;
    title: string;
    paragraphs: string[];
    citations: GuideCitation[];
  }[];
  globalCitations: GuideCitation[]; // deduplicated across sections
}

type GuideCitation = {
  number: number;
  sourceUrl: string;
  sourceName: string;
  retrievedAt: string;
  agentWhoAdded: string;
}
```

Save citations to guide_section_citations table.

Replace the call site of enrichGuide in guide-generator.ts. Now generateGuide() calls the new Composer pipeline instead of the single LLM enrichment.

Don't delete the old enrichGuide. Keep it as the v1 path for the comparison page if needed.

Run pnpm tsc --noEmit.

When you're done, generate a guide for the German posting profile. Show me citations across sections, with no duplicate citation numbers, and show me the consistency pass output.
```

---

## Prompt 6.2 — Build citation rendering in the guide UI

```
Update app/(app)/guides/[id]/page.tsx and components/guide-section.tsx to render inline citations.

In each guide section's content:
- Render [1] [2] [3] markers as small superscript numbered citations.
- Each marker is a hoverable + clickable element.
- Hover shows a small tooltip: source name + retrieval timestamp.
- Click opens the official source URL in a new tab.
- If retrievedAt is older than 7 days, the citation marker has a small "verify" badge (different color) with a hover tooltip "Source last verified [N] days ago — please verify recent changes".

Build components/citation-marker.tsx:
```
type CitationMarkerProps = {
  citation: GuideCitation;
}
```

Build components/sources-panel.tsx:
A sidebar panel listing all sources used in the current guide. Deduplicated. Each source has:
- Source name
- URL (clickable)
- Retrieved at
- Used in sections: [list of section titles]
- Verify recent changes button (re-scrapes the source and updates last_verified_at)

The sources-panel can be opened via a "Sources" button at the top of each guide.

Update guide-section.tsx to accept paragraphs with citations (the data shape from ComposedGuide). Render each paragraph with citation markers inline.

Update the existing source-card.tsx component to be reused inside sources-panel.

Run pnpm tsc --noEmit.

When you're done, walk me through a guide with citations. Click a citation to verify it goes to the actual official source. Open the sources panel.
```

---

## Prompt 6.3 — Build real-time guide generation visualization

```
Build the real-time guide visualization that runs while the Composer + section writers are working.

When research finishes (stage transitions to ready_for_pre_departure), the agent panels page (Phase 3) starts to fade. Below them, an empty guide skeleton appears with section placeholders.

As each section writer finishes, the corresponding section's content fills in with a brief highlight animation. The agent's name is briefly shown in the section header ("Visa Section Writer just finished").

After all section writers complete and the Composer pass finishes, the page settles into the final guide rendering.

Use Server-Sent Events (already set up in Phase 3) to push section completion events from the orchestration backend to the frontend.

Add components/guide-section-skeleton.tsx that shows a placeholder for a section while it's being written.

The visualization should feel intentional — it's the user watching their personal guide being written by a team. Not just a loading screen.

Don't break the existing /guides/[id] page. The visualization is a transitional state shown only during the brief window between research completion and pre-departure stage.

Run pnpm tsc --noEmit.

When you're done, walk me through the visualization for one full research-to-guide flow.
```

---

# PHASE 7 — Multi-agent settling-in checklist

## Prompt 7.1 — Build the settling-in domain specialists

```
Build settling-in domain specialists in lib/agents/settling_in/.

Each specialist generates 5-10 tasks specific to the user's situation in the destination country. Each task uses the existing schema (matches what dag-validator.ts and settling-in-task-card expect).

Task structure (matches existing settling_in_tasks schema):
- title
- description
- depends_on (array of task IDs that must complete first)
- deadline_days (days after arrival, with legal-requirement marking for hard deadlines like Sweden's 7-day folkbokföring)
- is_legal_requirement (boolean)
- steps (numbered actions array)
- documents_needed (array, references documents from research)
- official_link (government portal URL)
- estimated_time (visit duration)
- cost (any fees)
- agent_who_added_it (audit trail)

Specialists:

settling_in/registration.ts:
- Population register registration (folkbokföring/Anmeldung) — legal deadline (7 days Sweden, 14 days Germany)
- Personnummer/Lebensnummer issuance (handled via folkbokföring in Sweden)
- Residence permit physical pickup (if visa was approved abroad)
- ID card application (BankID requires this in Sweden)

settling_in/banking.ts:
- Bank account opening — depends on registration completion
- BankID enrollment (Sweden) — depends on bank account + ID
- Salary transfer setup
- Pension fund selection

settling_in/housing.ts:
- Rental contract finalization (if not done remotely)
- Utility account setup (electricity, gas, internet, water)
- Home insurance enrollment
- Waste sorting registration (Sweden has detailed sorting; Germany has Pfand)
- Mailbox / post forwarding from origin

settling_in/healthcare.ts:
- Insurance card pickup (Försäkringskassan/Krankenkasse)
- Vårdcentral/Hausarzt registration (primary care)
- Dentist registration
- Pharmacy registration if prescription continuity
- Children's pediatrician registration (if children)
- Vaccination record transfer
- Specialist/chronic condition follow-up (if chronic_condition_description)

settling_in/employment.ts:
- Work permit start (if applicable)
- Employment contract registration with authority
- Social security number for employment
- Payroll setup with employer
- A1 certificate filed with employer (if posted worker)
- Trade union enrollment (Sweden has high union density)

settling_in/transport.ts:
- Public transit pass (SL-card Stockholm, BVG-Karte Berlin)
- Driver's license exchange/conversion (EU mutual recognition vs. non-EU)
- Vehicle registration (if bringing vehicle)
- Bicycle registration if applicable

settling_in/family.ts:
- School enrollment confirmation in person (already arranged pre-departure)
- After-school care enrollment (fritidshem / Hort)
- Family doctor for kids
- Vaccination record submission to school
- Children's BankID enrollment (some banks allow from age 7)

settling_in/tax.ts:
- Tax authority registration (Skatteverket / Finanzamt)
- Tax residency declaration
- F-tax slip if self-employed
- SINK application if temporary worker (Sweden)
- Steuerklasse selection (Germany)

Each specialist function:
```
async function generate(profile: Profile, arrivalDate: Date): Promise<SettlingInTaskGroup>

type SettlingInTaskGroup = {
  domain: string;
  tasks: SettlingTask[];
  agentName: string;
}
```

Use Sonnet 4.5 for all settling-in specialists.

Run pnpm tsc --noEmit.

When you're done, run all settling-in specialists for the German family of 4 posted to Stockholm. Show me the tasks each generated.
```

---

## Prompt 7.2 — Build the settling-in coordinator and DAG stitching

```
Build lib/agents/settling_in/coordinator.ts.

The Coordinator collects all specialist outputs and stitches them into a single DAG using existing dag-validator.ts.

Function:
```
async function stitchDAG(
  taskGroups: SettlingInTaskGroup[],
  profile: Profile,
  arrivalDate: Date
): Promise<{ tasks: SettlingTask[], dagValid: boolean, urgentDeadlines: SettlingTask[] }>
```

Logic:
1. Collect all tasks from all task groups.
2. Resolve cross-domain dependencies. Examples:
   - Banking tasks depend on Registration tasks (need personnummer).
   - Salary setup depends on Banking tasks.
   - School in-person registration depends on Address registration (need address proof).
   - Transit pass depends on Address registration (need to register at the resident pass).
   - BankID depends on Bank account + Government ID.
3. Topologically sort tasks using dag-validator.ts.
4. Compute deadlines (deadline_days after arrival_date).
5. Identify urgent deadlines: hard legal deadlines within 7-14 days (folkbokföring in Sweden is 7 days, Anmeldung in Germany is 14 days).
6. Mark legal requirements vs. recommended.
7. Return the sorted task list.

Replace lib/gomate/settling-in-generator.ts logic. The new generator function:
1. Calls all settling-in specialists in parallel.
2. Calls Coordinator to stitch DAG.
3. Saves all tasks to settling_in_tasks table (existing migration).
4. Returns the task list.

Update app/api/settling-in/generate/route.ts to use the new generator. The route's behavior:
1. Auth check.
2. Verify plan stage = "arrived" (this only runs when user clicks "I have arrived").
3. Call new generator.
4. Set settling_in_generation_completed = now() on relocation_plans.
5. Return tasks.

Update app/api/settling-in/arrive/route.ts:
1. User clicks "I have arrived" → arrival_date = now() (or user-provided date), stage = "arrived".
2. Auto-trigger settling-in generation (this transition is automatic — DAG generates immediately when user marks arrival).

Existing components (settling-in-task-card.tsx, compliance-timeline.tsx, compliance-alerts.tsx) work without changes — the data model is the same.

Run pnpm tsc --noEmit.

When you're done, simulate "I have arrived" for the German family. Show me the full generated DAG with topological order, dependencies (banking blocked until registration; salary blocked until banking; school in-person blocked until address registration), and deadlines (7-day folkbokföring most urgent).
```

---

# PHASE 8 — Polish post-arrival chat

## Prompt 8.1 — Polish post-arrival chat with smart suggestions and citations

```
v1 already has post-arrival chat via buildPostArrivalSystemPrompt in lib/gomate/system-prompt.ts. Settling-in coach role with [TASK_DONE:exact title] markers. This works.

What's missing: smart suggestion chips, multi-source context routing, citation enforcement.

In app/(app)/chat/page.tsx, when stage = "arrived", switch to a different rendering mode:
- Mascot becomes a small avatar on assistant messages (not center stage).
- Input becomes a traditional textarea (not tied to single field).
- Above input: smart-suggestion-chips component.
- Message list is traditional chat (user right, assistant left with mascot avatar).

components/smart-suggestion-chips.tsx:
The chips show context-aware suggestions based on user's settling-in state. Build a function getSmartSuggestions(profile, settlingInTasks, recentWellbeing) that returns 3-5 chip suggestions:

If overdue compliance task exists: "Help me complete [task name]"
If upcoming deadline within 7 days: "What do I need for [task]?"
If recently arrived (<14 days): "Find an English-speaking doctor near me", "Where do I register my address?"
If starting work soon: "What documents do I need for the first day?"
If wellbeing low (recent struggling/overwhelmed check-in): "I'm feeling overwhelmed — what should I prioritize?"
General: "What's my next priority?", "Show me my deadline list"

Each chip when clicked populates the input (user can edit before sending).

Update buildPostArrivalSystemPrompt to require citations on every response that mentions a task, deadline, or regulatory requirement. Use [1] [2] inline marker format (consistent with guide).

Soft context routing: when user's question matches a domain (visa, banking, healthcare, schools, taxes, posted worker), the chat agent dispatches a focused mini-research call to the relevant specialist. The specialist returns a citation-grounded answer that the chat agent renders in its response.

Implementation:
1. On user message, classify domain via simple keyword matching or quick LLM classification.
2. If domain detected: call the specialist's run() function with a constrained context (just the user question).
3. Specialist returns SpecialistOutput.
4. Chat agent receives the output and incorporates it into its response with citations.

Use Sonnet 4.6 for the chat agent (warm conversational, same as Question Director).

Persist chat history in existing chat_messages table from migration 025. Don't create new tables. Keep last 50 messages per user; older messages summarized into context summary.

Run pnpm tsc --noEmit.

When you're done, walk me through three post-arrival questions for the German family in Stockholm:
1. "Where do I register the kids for school in-person?" → smart suggestion appears, response cites Skolverket.
2. "How do I get my BankID?" → response explains personnummer → bank → BankID dependency chain with citations.
3. "I'm feeling overwhelmed by all the paperwork" → supportive response, references current deadlines, prioritizes, doesn't push more tasks.
```

---

# PHASE 9 — Comparison + debug + polish + ship

## Prompt 9.1 — Build the architecture transparency page

```
Build app/(app)/compare/page.tsx. This is an ARCHITECTURE TRANSPARENCY page — it shows curious users (and Replit Buildathon judges) how GoMate's multi-agent extraction differs from naive single-agent extraction. It is NOT a value-prop marketing page. The "replace your $8k consultant" pitch lives on gomaterelocate.com, not here. Do not put pricing comparisons, consultant-cost call-outs, or marketing copy on this page.

Page title: "How GoMate's extraction works" (NOT "GoMate vs Traditional Consultants" or anything similar).

Single input box at the top. User types one thing — e.g. "I'm 30 years old, planning to move to Sweden for work next year with my wife and two kids."

When submitted, render two columns side by side.

Left column "Single-agent extraction (the legacy approach)":
- Calls /api/chat-v1 with the input (the moved-aside v1 logic).
- Shows fields the legacy single-agent pipeline extracted with the literal-string bugs visible.
- Highlights errors (e.g., "I'm 30 years old" appearing in birth_year field, missing inferred fields, "additional fields detected" from extraction not happening).

Right column "Multi-agent extraction (how GoMate works now)":
- Calls the new multi-agent pipeline.
- Shows what each agent did with timestamps.
- Shows audit trail (which agent extracted what, with confidence).
- Shows clean profile state the multi-agent pipeline produces.

Below columns: brief technical explanation. "Naive extraction uses one AI to do everything. GoMate splits the work across focused agents — Extractor, Validator, Profile Writer, Question Director. Each agent has one responsibility, a bounded schema, and an audit trail. Here's what changed and why it matters for accuracy."

Add app/api/comparison/route.ts that runs both pipelines server-side (don't make user wait for two sequential calls — run in parallel).

Add a "Try this example" section with 3 pre-canned examples user can click:
- Filipino fiancée moving to Sweden as dependent
- German engineer posted to Stockholm for 18 months
- American digital nomad to Spain solo

Build components/comparison-view.tsx for the two-column layout.

Run pnpm tsc --noEmit.

When you're done, run the comparison with three different inputs. Show me the side-by-side outputs.
```

---

## Prompt 9.2 — Build the agent debug drawer

```
Build the agent debug drawer that's collapsed at the bottom of every page.

components/agent-debug-drawer.tsx:
- Collapsed by default at the bottom of the screen with a small handle/tab labeled "Agent activity"
- Click handle to expand into a panel.
- Expanded panel shows real-time agent activity across the app:
  - Timestamp
  - Agent name
  - Phase (extraction / validation / research / enrichment / settling-in / chat / pre-departure)
  - Truncated prompt (first 200 chars) with show-more
  - Truncated response (first 200 chars) with show-more
  - Tools called
  - Validation pass/fail
  - Retry counts
  - Tokens used
  - Wall-clock time
- Filter options:
  - Failures only
  - Specific agent only
  - Last 5 minutes only
  - Specific phase only

Data source: agent_run_log table. Add app/api/agent-debug-log/route.ts (GET with filter query params) that streams entries via Server-Sent Events.

The drawer is for power users and judges who want to verify the multi-agent architecture is real, not theater. Default closed.

Add the drawer to the root layout (app/(app)/layout.tsx) so it's available on every page.

Run pnpm tsc --noEmit.

When you're done, walk me through opening the drawer during an active research run. I want to see live agent activity scrolling.
```

---

## Prompt 9.3 — Consolidate Pro and Pro+ into a single Pro tier

```
Currently lib/gomate/tier.ts and the rest of the app reference three tiers: free, pro, pro_plus. The pro vs pro_plus distinction creates friction — users have to decide between $189 one-time (Pro for guide and visa research) and $39/mo (Pro+ for ongoing post-arrival features). The features overlap and the choice is confusing.

Consolidate to two tiers: free and pro. There is no pro_plus anymore.

Step 1: Update lib/gomate/tier.ts.
- Remove pro_plus from the SubscriptionTier type. The type becomes: "free" | "pro".
- Update hasFeatureAccess() so all previously-pro_plus-gated features are now pro-accessible. There are no pro_plus-only features anymore. Specifically these features become Pro-accessible: post_arrival_assistant, settling_in_full, compliance_alerts, plan_consistency, multi_plan, wellbeing_checkins, free_chat_post_arrival.
- Free tier keeps: profile_interview, basic_country_overview, single_country_limit (only one active relocation plan).

Step 2: Add a database migration that updates any existing users with tier=pro_plus to tier=pro. Don't delete the column or change the schema — just migrate values:
```sql
UPDATE users SET subscription_tier = 'pro' WHERE subscription_tier = 'pro_plus';
```

Step 3: Update components/upgrade-modal.tsx. Remove the Pro vs Pro+ choice. Show a single Pro upgrade with:
- Title: "Upgrade to Pro"
- Pricing: $39/month OR $299/year (annual saves ~36%)
- Single CTA: "Get Pro" or "Get Pro for the year"
- Bullet list of features included (every feature the app has).

Step 4: Update components/tier-gate.tsx. Simplify the gating to just: free vs pro. Don't reference pro_plus anywhere.

Step 5: Update app/api/subscription/route.ts. Simplify subscription lifecycle to single Pro tier. Subscription metadata supports billing_cycle: "monthly" | "annual". Annual gets the discount automatically.

Step 6: Update the Stripe mock / Stripe integration to only define two products: Free ($0) and Pro ($39/mo or $299/yr). Find any Stripe-related files (look for stripe in lib/, app/api/, or environment config). Remove the pro_plus product/price definitions. Update mock checkout flows to only support the two-tier Free/Pro choice. If there's a Stripe webhook handler, simplify it to only process pro tier upgrades and downgrades — no pro_plus events.

Step 7: Search the codebase for "pro_plus" string references. Remove or update each. There should be zero references to pro_plus anywhere in code after this prompt — not in tier checks, not in Stripe products, not in pricing displays, not in upgrade modal copy, not in subscription metadata.

Step 8: Update any pricing displays (e.g., on landing page or settings) to show: Free $0, Pro $39/mo or $299/yr.

Step 9: For the buildathon, override tier.ts so getUserTier() always returns "pro" regardless of actual subscription state. Judges should be able to test full Pro experience without paying. Add a comment in the code explaining this is the buildathon override.

Step 10: After all changes, run pnpm tsc --noEmit and verify no type errors. Run a search for "pro_plus" — should return zero results.

When you're done, walk me through:
1. The Free user experience — what they can and can't do.
2. The Pro user experience — confirm they have access to every feature the app supports.
3. The upgrade modal showing single Pro CTA with $39/mo or $299/yr.
4. The Stripe mock showing only Free and Pro products.
5. Confirmation that pro_plus references are gone from the codebase (grep should be empty).
```

---

## Prompt 9.4 — Design overhaul: from default-SaaS to editorial-warm premium

```
The current GoMate v1 design feels generic. White cards on off-white background, pastel Lucide icons, default Inter typography throughout, isolated dark green sidebar, no visual hierarchy beyond size. It looks like a Lovable/v0 template output — the exact AI-slop pattern we're claiming to fix elsewhere in our positioning. For a product that handles someone's family's international relocation and charges $39/month, this feels like a free tool.

Transform the design language to "editorial-warm premium with sharp precision". Think Substack newsletters meets Posthog dashboard meets Notion-warm meets Stripe-precision. The product handles emotional, high-stakes work (visas, taxes, kids' schools) — the design should communicate trust, gravity, and human warmth, not generic dashboard.

Direction principles:
1. Editorial confidence — large confident headlines, comfortable body text, varied typographic hierarchy
2. Warm gravity — warm off-white backgrounds with subtle grain texture, not pure white sterile
3. Distinctive identity — when someone sees a screenshot they should know it's GoMate, not "another AI SaaS"
4. Trust signals — confident copy, source citations visually emphasized, audit traceability surfaced
5. Restrained motion — subtle hover lifts on cards, color shifts on interactive elements, easing transitions

Specific changes:

Typography:
- Headings: switch to a serif or display sans that has editorial gravitas. Specifically use a font like Source Serif Pro for h1-h2 section headers, or Geist (or similar geometric display) at heavy weights with tighter tracking. Body stays as a clean sans (Inter or Geist Sans is fine).
- Increase typographic contrast: h1 should be visibly large (48-64px), h2 noticeably smaller but still confident (28-32px), body 16px, captions 13px. Current design has too-similar sizes everywhere.
- Use ALL CAPS small-caps on small section labels with letter-spacing for editorial feel ("LATEST IN AI", "BUDGET BREAKDOWN", "VISA REQUIREMENTS").
- Add font-feature-settings for tabular numerals on all numbers in tables, costs, deadlines.

Color system — replace the current pastel-icon-on-white pattern:
- Background: off-white with warm undertone (#FAFAF6 or #F8F5F0). Add very subtle grain texture using SVG noise pattern overlay at 2% opacity.
- Primary action color: a confident, warm coral or amber (#E85D3C or #F59E0B) — not pastel. Used sparingly for primary CTAs, urgent deadlines, key highlights.
- Sidebar: keep dark green but deepen it. Add a subtle gradient from top to bottom (slightly darker at bottom). Add a small grain texture in the sidebar to break up flatness.
- Cards: instead of pure white, use very subtle warm neutrals. Section-specific tints — warm cream for cultural sections, cool stone for compliance sections, soft blush for healthcare. The variation is subtle (5% saturation) but breaks the white monotony.
- Borders: replace generic shadcn borders with a slightly warmer, slightly stronger border (rgba(120,90,60,0.15) instead of pure neutral).
- Status colors: replace pastel green/yellow/red with more confident saturated versions for badges and alerts. Specifically: success green deeper and slightly olive, warning amber more honey-toned, error red slightly burgundy. These read more "premium" than candy-pastel.

Iconography:
- Replace generic Lucide icons in prominent places with custom or curated SVG icons that feel distinctive. Specifically: dashboard cards (Destination, Purpose, Profile, Timeline) should have custom icons — not pale-pastel-Lucide-circles. Use line-art style icons with consistent stroke weight, possibly with subtle color fill on hover.
- Country flags should keep current rendering but pair with destination cards in a more integrated way (full-bleed flag at top of destination card, not isolated).
- Status indicators (overdue, urgent, approaching) get distinctive treatments — overdue is a small pulsing dot, urgent is solid colored, approaching is outline. Don't use the same icon shape with different colors.

Layout & spacing:
- Increase whitespace between major sections. Current design feels packed.
- Use 12-column grid more rigorously for card layouts — current layout has cards of slightly different widths that look haphazard.
- Section dividers should be thin warm-toned hairlines, not generic gray.
- The "Amira's move at a glance" hero card is the right idea but needs MORE — overlay a destination image (London skyline at warm sunrise tone) at low opacity behind the green gradient to add depth and place-specificity.

Motion & interactions:
- Cards: subtle hover lift (translateY -2px, shadow expansion) on all clickable cards.
- Buttons: scale down to 0.98 on press, color shift on hover.
- Section transitions: when navigating between dashboard sections or expanding cards, use 200-300ms eased transitions.
- Loading states: replace generic skeletons with subtle shimmer or pulse using brand colors.
- Real-time updates (audit trail, agent panels): elements appear with brief highlight animation (yellow flash fading to normal) to draw attention to changes.

Brand-specific touches:
- The mascot / GoMate avatar should be more prominent on the landing experience and in the chat. Currently it's tiny and forgotten.
- Add a small "trust badge" near the dashboard hero: "Grounded in [N] official sources, last verified [date]" — visualizes the source-citation moat.
- Page transitions and CTAs should feel deliberate, not snappy-instant. Slightly slower easing communicates premium.
- Add a subtle "Built by GoMate" footer with a small mark, but elevate it slightly — currently it reads like a budget-product disclaimer.

Specific page redesigns:

Dashboard hero (currently "Amira's move at a glance"):
- Larger headline (52-64px, serif heading or geometric display)
- Subhead 18-20px in body sans
- Right-aligned "Plan locked" + "Ask questions" pills should have clearer visual hierarchy — primary action stronger, secondary action understated
- Background: gradient with destination image overlay at 8% opacity, slight warm tint at top
- Add a small status row below subhead: "✦ Updated 4 days ago · 12 sources cited · Confidence: high"

Relocation profile section (the multi-card progress display):
- Currently each progress section ("Journey Overview", "Study Details", etc.) is a uniform white card with the same visual weight. Differentiate them — different left-border accent colors per category, subtle background tint, smaller bolder progress percentage with confident typography.
- The 100% completed state should feel earned — small subtle celebration micro-animation or color shift, not just a green pill.

Document checklist sidebar:
- Currently very plain. Make it feel like an actionable to-do list with priority emphasis. Required items get a small red dot, recommended items a softer indicator. The "Complete Checklist" CTA at bottom should feel weighty — primary green button is fine but add a subtle gradient and shadow.

Visa Research card:
- Currently looks like a basic info card. Add a "trust frame" — official source attribution in the header, citation badges next to claims. Make it look like a real document, not a generated paragraph.

Budget Plan section:
- Currently the savings goal card and cost of living card sit side by side as equal whites. Differentiate — savings goal is "your action" (warmer tone, more visual weight), cost of living is "data we found" (cooler tone, lighter, with the Numbeo source badge clearer).

Implementation:
- Update tailwind.config.ts with new color tokens (warm-cream, dark-green-deep, primary-coral, etc).
- Update globals.css with the warm undertone background and subtle grain pattern.
- Update components/page-header.tsx, components/stat-card.tsx, components/info-card.tsx, components/budget-card.tsx, components/visa-routes-card.tsx, components/local-requirements-card.tsx, components/document-progress-card.tsx, components/cost-of-living-card.tsx with the new design language.
- Add a new lib/design-tokens.ts file exporting the design system constants for consistency.
- For typography, use next/font to load the serif heading font (Source Serif Pro or similar) and ensure it's applied to h1, h2 site-wide.

Don't go full redesign — preserve the information architecture and component structure. We're upgrading the visual language and motion, not rebuilding the dashboard. Existing functionality must continue to work.

After the design pass, walk me through:
1. The dashboard with the new warm background, grain texture, and updated hero
2. A sample card hover interaction
3. The new typography hierarchy on a guide page
4. The audit-trail popover with the new design language
5. A side-by-side comparison: before screenshot vs after screenshot for the dashboard

The before/after design comparison is itself a buildathon Progress score artifact (33% weight) — capture it as part of submission video.
```

---

## Prompt 9.5 — UI consolidation: Visa page + Checklist page

```
The current sidebar has separate items for Visa Tracker, Settling In, and (sometimes) Documents. Each is its own page. This is fragmented — a user looking for "what do I do next" has to navigate three places. We consolidate into two:

1. **Visa** (replaces Visa Tracker) — everything visa-related lives here.
2. **Checklist** (replaces Settling In) — three tabs: Pre-move, Post-move, Documents.

The standalone /documents page goes away. Its content moves into the Documents tab inside Checklist.

Sidebar changes (in app/(app)/layout.tsx):
- Replace "Visa Tracker" item with "Visa"
- Replace "Settling In" item with "Checklist"
- Remove "Documents" item if it exists separately
- Order: Dashboard, Chat, Visa, Checklist, Guides, Settings

The Visa page (rename app/(app)/visa-tracker/page.tsx to app/(app)/visa/page.tsx, update routes everywhere):

The page becomes a single comprehensive visa workspace with these sections:

1. Visa Research summary at top — pathway recommendation from Visa Specialist agent. Inline citations to official immigration source URLs (e.g., Migrationsverket, UK gov.uk visas, US uscis.gov). Click to expand for full pathway analysis.

2. Selected Visa Pathway — once user has chosen a pathway, show: requirements, processing time, fees, required documents (with status indicators per document), application steps, current status (not_started / preparing / submitted / awaiting_decision / approved / rejected).

3. Visa-specific Documents — subset of the overall documents that are required for THIS visa pathway. Each document has: name, why it's needed for this pathway, where to obtain in origin country, apostille requirement, translation requirement, submission destination, current status. Status updates here also update the master Documents tab in Checklist.

4. Application Status Timeline — visual stepper showing: prepared → submitted → awaiting decision → approved/rejected. Pull from existing visa-status-stepper component.

5. Deadlines — visa-related deadlines pulled into one view (visa application deadline, biometrics appointment, decision expected, visa pickup window). Use existing visa-deadline-card component.

6. Posted Worker Compliance — only if posting_or_secondment === "yes". Shows: A1 certificate status (or CoC for non-EU treaty), Posted Worker Declaration filing status, employer registration status. This is its own section because it has critical legal deadlines (PWD must be filed BEFORE work starts).

7. Application History — once submitted, shows submission timeline with timestamps and any communications received.

The Checklist page (transform app/(app)/settling-in/page.tsx to app/(app)/checklist/page.tsx, update routes):

The page has three tabs at the top:

Tab 1: Pre-move
- Renders the pre-departure timeline (the work from Phase 5).
- Horizontal timeline organized by weeks-before-move.
- Filter: show all / show overdue / show in-progress / show next 2 weeks.
- Each action has status checkbox (not_started / in_progress / complete / blocked / skipped).
- Click action to expand for full details, source links, pre-filled forms.

Tab 2: Post-move
- Renders the settling-in DAG (the work from Phase 7).
- Sort by deadline urgency or by category.
- Filter: legal requirements only / show all / show by domain (registration / banking / housing / healthcare / employment / transport / family / tax).
- Each task uses existing settling-in-task-card component.
- Show dependencies clearly — task is blocked if its dependencies aren't complete.

Tab 3: Documents
- Comprehensive document checklist for the user's specific situation.
- Sections by domain: Personal, Family, School (if study), Work (if work), Posted Worker (if posting), Vehicle (if bringing), Pet (if bringing).
- Each document item shows:
  - Name
  - Why it's needed (which visa pathway, which task, which compliance)
  - Where to obtain in origin country (with link)
  - Apostille requirement (yes/no, where to get apostille, processing time)
  - Translation requirement (yes/no, certified vs sworn translator)
  - Submission destination (consulate URL, online portal URL, or in-person office address)
  - Current status (not_started / requested / received / apostilled / translated / submitted)
  - Lead time estimate (some documents take 6-8 weeks to process)
  - Status update buttons (Mark requested / Mark received / Mark apostilled / etc.)

The Documents tab is THE comprehensive document workspace. Every document the user needs anywhere in their relocation flow surfaces here. Marking a document complete here updates its status everywhere it's referenced (visa page, pre-move tasks, post-move tasks).

Document types to support — Documents Specialist agent must output ALL of these as applicable:

Personal (always):
- Passport (with 6+ months validity check)
- National ID / driver's license (origin)
- Police clearance certificate (apostilled if required by destination)
- Medical examination certificate (if destination requires)
- Health insurance proof
- Vaccination records (if destination requires)
- Bank statements (financial proof for visa)
- Tax returns from origin (financial proof)

Family (if moving with family):
- Marriage certificate (apostilled + translated if required by destination)
- Birth certificates of children (apostilled + translated)
- Family book / Familjebok (some countries)
- Adoption papers if applicable
- Custody documents if children moving without one parent

School (if purpose === "study" OR children_count > 0):
- Acceptance letter from school / university
- Diploma from prior education (apostilled + translated)
- Academic transcripts (apostilled)
- English language proof (IELTS / TOEFL / Cambridge if required)
- Study purpose statement
- Financial proof for student visa (savings_available / sponsor letter)
- Children's school enrollment confirmation
- Children's school transfer / records request

Work (if purpose === "work"):
- Employment contract (signed)
- Employer letter / job offer letter
- Salary certificate
- Professional license / qualifications (apostilled)
- Work permit application (if applicable)
- EU Blue Card application (if applicable)

Posted Worker (if posting_or_secondment === "yes"):
- A1 certificate application receipt (origin's social security agency for EU postings)
- A1 certificate when received
- Certificate of Coverage / CoC (for non-EU bilateral treaties — US-Sweden, India-Sweden, etc.)
- Posted Worker Declaration / PWD receipt (filed with destination's labor authority — Arbetsmiljöverket in Sweden, ZOLL in Germany)
- Employer contact person registration in destination
- Posting employer's employer letter

Pet (if pets ≠ "none"):
- Pet passport (EU)
- Microchip certificate
- Vaccination records (rabies, others)
- Veterinary export certificate
- Import permit (if destination requires)

Vehicle (if bringing_vehicle === "yes"):
- Vehicle title / registration certificate (origin)
- Insurance documents
- Emissions certificate (for EU import)
- Customs declaration
- Bill of sale / value documentation

Departure-side (if origin has exit tax):
- Departure tax declaration (filed with origin tax authority)
- Asset valuation reports if needed
- Pension fund decisions documentation
- Origin address change / Abmeldung receipt (Germany)

Each document is generated by the Documents Specialist agent based on the user's specific profile. The agent:
1. Reads the user's selected visa pathway
2. Reads conditional profile fields (children_count, posting_or_secondment, bringing_vehicle, etc.)
3. Generates the relevant document list with all required fields above
4. Cross-references with the Visa Specialist's pathway requirements
5. Ensures no contradictions (if Visa Specialist requires "police clearance from origin", Documents Specialist outputs the apostille chain for it)

The Documents Specialist agent must run during research orchestration (Phase 3) and update its output if the user changes their visa pathway selection or any conditional profile field. Each generated document gets stored in a new database table or extended document_statuses JSONB column on relocation_plans.

Migration: extend relocation_plans.document_statuses (existing JSONB column) to include all new document types. OR create a new documents table with: id, profile_id, document_type, document_name, why_needed, where_to_obtain, apostille_required, translation_required, submission_destination, lead_time_days, status, agent_who_added_it, created_at, updated_at.

UI consistency:
- Visa page and Checklist page share the same audit-icon system from Phase 4.
- Document items have audit icons showing which specialist added them.
- Citations on every claim within the Visa page (visa requirements, fees, processing times).
- Hover states and motion consistent with the design language from Prompt 9.4.

Existing components to reuse:
- visa-routes-card, visa-status-stepper, visa-deadline-card, visa-research-card → all live inside the new Visa page
- visa-document-checklist → expand to full Documents tab
- settling-in-task-card → used in Post-move tab
- pre-departure-timeline → used in Pre-move tab
- interactive-document-checklist → used in Documents tab (or replace with new comprehensive component)
- compliance-alerts → surfaced both in Checklist page and on Dashboard

Routing:
- /visa-tracker → 301 redirect to /visa (preserve existing bookmarks)
- /settling-in → 301 redirect to /checklist
- /documents → 301 redirect to /checklist?tab=documents
- All internal links updated to new routes.

Critical dashboard cleanup — Document Checklist is currently rendered as a sidebar widget ON the dashboard page (the right-side panel showing "2 critical documents remaining" with priority items like "Valid Passport", "Health Insurance Proof", "Biometric Photos"). This is wrong. Document Checklist belongs in the Checklist page Documents tab, not on the dashboard.

Remove from app/(app)/dashboard/page.tsx:
- The Document Checklist sidebar widget entirely.
- The interactive-document-checklist component instance on the dashboard.
- The document-progress-card if it currently renders the full checklist (a small "X of Y documents complete" stat tile is fine — it links to /checklist?tab=documents).

What stays on the dashboard for documents:
- A small status tile showing overall document progress: "8 of 14 documents ready" with a single CTA "Open Checklist". That's it. The detailed list is one click away.
- If there are urgent document deadlines (e.g., apostille processing time means user needs to start NOW), surface as a compliance alert in the alerts strip — same pattern as other deadline alerts.

The dashboard becomes cleaner — it's a status overview, not a document workspace. Detailed work happens on /visa and /checklist. The dashboard shows: hero (move at a glance), profile summary, key stats (destination, purpose, profile completion, timeline), critical alerts, and small status tiles linking to the deeper pages. That's it.

Same principle: the Visa Tracker sidebar widget on the dashboard (showing "Track your visa application status, deadlines, and required documents") should also be reduced to a small status tile linking to /visa. Don't render the full visa application stepper on the dashboard.

When you're done, walk me through the new sidebar structure. Then walk me through:
1. Visa page for the German posting profile (should show A1 + PWD + employer registration status prominently in the Posted Worker Compliance section)
2. Checklist page Pre-move tab for the Filipino dependent profile (should show apostille chains for marriage cert, birth certs, with proper dependencies)
3. Checklist page Documents tab for the Filipino dependent (should show ALL document types: personal, family — including marriage cert apostille and translation steps — but no school, work, pet, vehicle since those don't apply)
4. Checklist page Documents tab for the German posting (should show personal, family, work, posted-worker, but no pet/vehicle since not bringing)
```

---

## Prompt 9.6 — Add 7 missing specialists for true Relocation Consultant OS coverage

```
Real relocation agencies cover services we haven't built specialists for yet. We need to add seven more specialists to truly replace agency consultative work.

Build these in lib/agents/specialists/:

specialists/insurance.ts — Always dispatched.
- Reads destination, current_location, healthcare_needs, chronic_condition_description, insurance_needs_household, insurance_needs_vehicle, insurance_needs_life, gap_insurance_needed, bringing_vehicle, home_purchase_intent.
- Scrapes destination's insurance regulators and known expat insurance providers.
- Returns: required vs optional insurance for the user's situation. For each insurance type — health (already partially covered by Healthcare Navigator, but Insurance Specialist focuses on the financial-product side), household contents, vehicle (if bringing or planning to buy in destination), life, and gap insurance during transition (between origin coverage end and destination coverage start).
- Specifically calls out: international expat health insurance options for the gap period (Allianz Care, Cigna Global, Aetna International), local options in destination, employer-provided coverage if posted worker.
- Returns domainSpecificData with: requiredInsurance[], optionalInsurance[], gapPeriodOptions, expatProviders, localProviders.
- Uses Sonnet 4.5.

specialists/departure_origin.ts — Always dispatched.
- Reads current_location, origin_lease_status, origin_lease_termination_notice_days, target_date, bringing_personal_effects, working_remote_during_transition, register_with_origin_embassy_intent.
- Scrapes origin country's official sources for departure procedures: tax authority, population register (for deregistration), labor authority (if employed in origin), social security agency.
- Returns the full pre-departure origin checklist: lease termination steps and notice requirements, utility cancellation steps (electricity, gas, water, internet, mobile), origin population register deregistration (Abmeldung in Germany, mantalsskrivning utflyttning in Sweden, etc.), mail forwarding setup, bank notification, voter registration changes, subscription cancellations to consider, doctor/dentist deregistration, final tax filing in origin, pension fund decisions before departure.
- Returns domainSpecificData with: leaseTerminationSteps, utilityCancellationList, originDeregistrationSteps, mailForwardingProcess, bankNotifications, voterRegistrationChanges, finalTaxFiling, pensionDecisions.
- Each item gets a deadline timeline (X days before move, Y days before move, etc.) — feeds into Pre-departure timeline generator (Phase 5).
- Uses Sonnet 4.5.

specialists/goods_shipping.ts — Conditional on bringing_personal_effects === "yes".
- Reads current_location, destination, estimated_goods_volume_cubic_meters, goods_shipping_method, need_storage, target_date.
- Scrapes destination's customs authority for personal effects import rules.
- Returns: VAT-free allowance for personal effects (most countries allow duty-free import for genuine relocations under certain conditions — usually owned 6+ months prior, in user's possession), required customs declarations, recommended international moving companies for the country pair, transit insurance options, storage solutions if needed (origin-side and destination-side), packing strategy (what to ship vs sell vs donate vs self-carry).
- Returns domainSpecificData with: vatFreeAllowance, requiredCustomsForms, recommendedMovingCompanies, transitInsuranceOptions, storageOptions, packingStrategy.
- Uses Sonnet 4.5.

specialists/driver_license.ts — Conditional on driver_license_origin === "yes" AND driver_license_destination_intent !== "not_driving".
- Reads citizenship, current_location, destination, driver_license_destination_intent.
- Scrapes destination's driving license authority.
- Returns the license exchange/conversion path:
  - For EU-EU moves: mutual recognition (use existing license).
  - For EU/EEA-Switzerland: exchange (no test required typically, just paperwork within first year).
  - For non-EU origin to EU: country-specific — some have bilateral agreements (Australia-Sweden has one, Japan-Sweden has one, US-Sweden does NOT have one for license exchange — must take full Swedish driving test).
  - For non-EU to US: state-specific rules.
- Outputs: exchange/conversion process steps, document requirements (origin license, certified translation if not in destination's language, medical certificate if required, residency proof), validity period of original license post-arrival (often 6-12 months grace before mandatory exchange/conversion), cost, where to apply.
- Returns domainSpecificData with: exchangePath, documentRequirements, gracePeriodMonths, cost, whereToApply.
- Uses Sonnet 4.5.

specialists/embassy_registration.ts — Always dispatched (recommended for all expats).
- Reads citizenship, destination, target_city.
- Looks up origin country's embassy/consulate registration system in destination (Sweden: Svensklistan via Sweden Abroad, US: Smart Traveler Enrollment Program/STEP, UK: register with British Embassy, Germany: Konsularischer Schutz registration, Philippines: Department of Foreign Affairs registration for Filipinos abroad, etc.).
- Returns: registration process, what it gives the user (consular protection in emergencies, voting registration, official notices about origin policy changes that affect expats), URL to register, time required.
- Returns domainSpecificData with: embassyRegistrationProcess, benefits, registrationUrl, timeRequired.
- Uses Sonnet 4.5.

specialists/family_visa_cascade.ts — Conditional on visa_role === "primary" AND (children_count > 0 OR spouse_joining === "yes").
- Reads citizenship, destination, visa_role, children_count, spouse_joining, partner_citizenship.
- Reviews the Visa Specialist's pathway recommendation and the destination's family reunion rules.
- Returns: optimal visa application sequence — typically primary applicant submits and gets approved FIRST, then dependents submit with much simpler process referencing primary's approved status. The specialist outputs the recommended timing: when to submit primary application, when to submit dependent applications relative to primary's approval, what each dependent application needs (proof of primary's approval, proof of relationship, financial dependency proof), expected total elapsed time for whole family to be legally settled.
- Critical because: many users submit all family members simultaneously and create unnecessary delays/rejections.
- Returns domainSpecificData with: recommendedSequence, primaryApplicationTimeline, dependentApplicationsTiming, totalFamilyElapsedTime.
- Uses Opus 4.7 for sequencing logic.

specialists/healthcare_continuity.ts — Conditional on healthcare_needs !== "none" OR chronic_condition_description exists OR prescription_medications_list non-empty.
- This is a deeper specialist than Healthcare Navigator (which covers general healthcare access). Healthcare Continuity focuses on the transition gap and ongoing chronic care.
- Reads destination, chronic_condition_description, prescription_medications_list, healthcare_needs, english_speaking_doctor_required.
- Scrapes destination's drug regulatory authority (Läkemedelsverket in Sweden, BfArM in Germany, FDA equivalent), prescription medication availability database.
- Returns: prescription medication availability check (Adderall illegal in Japan, certain CBD products in UAE, codeine restricted in many countries, controlled substances need import permits), medical record transfer process (request from origin GP/specialist, format requirements for destination), chronic condition specialist availability in destination + waiting times, gap insurance options for the transition period (when origin coverage ends, before destination coverage starts).
- Returns domainSpecificData with: medicationAvailabilityChecks (per medication), medicalRecordTransferProcess, chronicSpecialistAvailability, gapInsuranceRecommendations.
- Uses Sonnet 4.5.

UPDATE lib/agents/coordinator.ts to add these to the dispatch logic:

Always dispatch (in addition to existing): insurance_specialist, departure_origin, embassy_registration.

Conditional dispatch (in addition to existing):
- goods_shipping if bringing_personal_effects === "yes"
- driver_license if driver_license_origin === "yes" AND driver_license_destination_intent !== "not_driving"
- family_visa_cascade if visa_role === "primary" AND (children_count > 0 OR spouse_joining === "yes")
- healthcare_continuity if healthcare_needs !== "none" OR chronic_condition_description exists OR prescription_medications_list non-empty

UPDATE Documents Specialist (lib/agents/specialists/documents.ts) to handle:

1. Apostille vs Consular Legalization distinction. The Hague Apostille Convention applies between member countries — apostille suffices. For non-Hague-Convention country pairs, consular legalization is required (different chain: notarization → foreign affairs ministry → destination consulate authentication). Documents Specialist must check Hague Convention status of both origin and destination and output the correct chain.

2. Children-specific documents:
- Children's passports (with 6+ months validity check)
- Children's vaccination records (some destinations require for inresa, all schools require for enrollment)
- Children's school transfer letters from current school
- Children's birth certificates (separate apostille chain per child)
- Custody documents if children moving without one parent

3. Power of Attorney documents (if user keeps property/business in origin):
- POA template for property management
- POA template for tax filing
- POA template for legal representation

4. Prescription medication import documents (if prescription_medications_list non-empty):
- Doctor's letter detailing prescriptions
- Original packaging recommendations
- Import permit application (some destinations require)

UPDATE Pre-departure timeline generator (lib/gomate/pre-departure.ts from Prompt 5.1) to incorporate timeline contributions from these new specialists:

Insurance Specialist contributes: gap insurance start date (overlap with origin coverage end), household insurance arrangement before lease starts.
Departure Origin Services contributes: lease termination notice (typically 90 days before move), utility cancellation timing, Abmeldung/deregistration (often must be done in last 14 days), mail forwarding setup, bank notifications.
Goods Shipping contributes: shipping company booking (8 weeks before for sea freight, 4 weeks for air), packing schedule, customs documentation prep.
Driver's License contributes: license translation if needed (before exchange application), grace period start counter from arrival.
Embassy Registration contributes: register before or shortly after arrival.
Family Visa Cascade contributes: dependent application submission timing (after primary approval).
Healthcare Continuity contributes: 3-month medication supply (if available), medical record transfer requests, gap insurance start.

Run pnpm tsc --noEmit. Confirm all new specialists compile and Coordinator dispatches them correctly.

When you're done, walk me through the specialists dispatched for these scenarios:
1. Filipino fiancée moving to Sweden as dependent: should add insurance, departure_origin, embassy, family_cascade (if she's the primary for her dependent visa? — clarify in dispatch logic)
2. German engineer posted to Stockholm with family + bringing dog + bringing furniture: should add insurance, departure_origin, goods_shipping (yes), driver_license (yes), embassy, family_cascade, healthcare_continuity (if any conditions)
3. American DN to Spain solo with chronic Type-1 diabetes and prescription insulin + bringing dog + leaving owned condo behind: should add insurance, departure_origin (deeper for owned property), goods_shipping (if bringing things), driver_license (if intends to drive), embassy, healthcare_continuity (deep for diabetes + insulin import rules)
```

---

## Prompt 9.7 — Plan consistency expansion + stage-aware wellbeing + critical missing features

```
We're adding three concerns in this prompt: expanded plan consistency rules, stage-aware wellbeing system, and three missing features (document upload, email reminders, document templates).

Step 1: Expand lib/gomate/plan-consistency.ts with 9 new rules.

Existing 8 rules: INCOME_BELOW_VISA_MINIMUM, BUDGET_BELOW_COL_MINIMUM, SAVINGS_INSUFFICIENT, ARRIVAL_BEFORE_VISA_READY, TASK_DEPENDENCY_VIOLATION, DOCUMENT_EXPIRED, TARGET_DATE_PASSED, MISSING_CRITICAL_DOCUMENTS.

Add these new rules:
- POSTING_EXCEEDS_A1_LIMIT: when posting_or_secondment === "yes" AND posting_duration_months > 24, flag that A1 max is 24 months and user needs transition plan.
- APOSTILLE_INSUFFICIENT_LEAD_TIME: check apostille processing time for origin country (varies, 3-8 weeks typical) against time-to-move-date. Flag if user has less time than apostille requires.
- SCHOOL_APPLICATION_TOO_LATE: for international schools with 6-12 month application timelines, if user's children_count > 0 and target_date is < 4 months and no school admission confirmed, flag.
- PET_VACCINATION_INSUFFICIENT_TIME: if pets ≠ "none" and pet_vaccination_status !== "current" and target_date < 30 days, flag (rabies vaccine has 21-day post-vaccination wait for EU travel).
- INSURANCE_GAP_DETECTED: if departure date is set and gap_insurance_needed = yes but no specific gap insurance arrangement noted, flag.
- FAMILY_VISA_SEQUENCING_SUBOPTIMAL: if family_visa_cascade specialist has flagged a sequencing issue (dependent applications too early before primary).
- PRESCRIPTION_RESTRICTED_IN_DESTINATION: if prescription_medications_list contains items that Healthcare Continuity specialist flagged as restricted/illegal in destination.
- DRIVER_LICENSE_VALIDITY_GAP: if driver_license_destination_intent === "exchange" or "convert" and origin license expires before grace period ends in destination.
- DEPARTURE_TAX_FILING_DEADLINE_PASSING: if origin has exit tax and filing deadline approaches without action.

Each rule follows existing pattern: severity, code, message, fix, relatedField. Use existing ConsistencyWarning type.

Step 2: Stage-aware wellbeing system.

Currently app/api/wellbeing/route.ts has 5 mood states (great/good/okay/struggling/overwhelmed) with static response messages.

Replace MOOD_MESSAGES with stage-aware messaging. The wellbeing API now reads the plan's current stage and tailors response:

Pre-departure stage (4-6 weeks before move):
- Acknowledge: pre-departure stress is intense, sleep loss and anxiety are common
- Resources: link to specific deadlines, suggest taking breaks from planning
- Crisis: if struggling/overwhelmed, surface "you have X weeks remaining and Y critical tasks left — let's prioritize together" rather than just dismiss

Arrival week (within 14 days post-arrival):
- Acknowledge: arrival shock and culture shock are real and expected
- Resources: focus on the one or two most urgent compliance tasks (not all 30)
- Crisis: if struggling, suggest contacting expat counseling (link to international counseling services), ground in basics (food, sleep, walk outside)

First 90 days post-arrival:
- Acknowledge: adjustment cycle (honeymoon → frustration → adjustment → acceptance) takes 6-18 months
- Resources: encourage community connection, language learning progress, small wins
- Trailing spouse specific: if spouse_joining=yes and the user is the trailing spouse, surface specific isolation support (career disruption is hard)
- Children: if children_count > 0, surface adjustment timeline expectations (kids take 6-18 months — patience required)

Settled (90+ days post-arrival):
- Acknowledge: integration is ongoing
- Resources: deeper community ties, language milestones
- Crisis: connect to ongoing support, professional counseling

Build stage-aware MOOD_MESSAGES as nested structure:
```
const STAGE_MOOD_MESSAGES = {
  pre_departure: { great: "...", good: "...", okay: "...", struggling: "...", overwhelmed: "..." },
  arrival_week: { great: "...", good: "...", okay: "...", struggling: "...", overwhelmed: "..." },
  first_90_days: { great: "...", good: "...", okay: "...", struggling: "...", overwhelmed: "..." },
  settled: { great: "...", good: "...", okay: "...", struggling: "...", overwhelmed: "..." }
};
```

Add family member tracking. Wellbeing-checkin currently records one mood for the user. Extend to optionally record mood for spouse and each child by name (if user wants to track family adjustment).

Add wellbeing widget on dashboard that surfaces if any family member's recent check-ins trend toward struggling/overwhelmed. Connect to a "Talk it through" button that opens post-arrival chat with empathetic context.

Step 3: Document upload functionality.

Build app/api/documents/upload/route.ts.

POST endpoint accepts file uploads (PDF, JPG, PNG) up to 10MB. Stores in Supabase Storage bucket 'user-documents'. Creates row in user_documents table:
- id, profile_id, document_type (matches Documents Specialist's document categorization), file_path, file_name, file_size, mime_type, uploaded_at, status ("uploaded" | "verified" | "rejected"), verification_notes (optional).

Add migration for user_documents table and Supabase Storage bucket setup.

In the Checklist page Documents tab, add upload button next to each document item. User clicks → file picker → uploads → file path stored. Documents Specialist agent (when re-running) can incorporate uploaded documents into status tracking.

Optional v2.5 (don't block on this): OCR/verification of uploaded documents via vision model — defer to post-buildathon.

Step 4: Email reminders for deadlines.

Add app/api/notifications/email/route.ts. Uses Resend (or fallback SendGrid) for sending. Create migration for user_email_preferences table:
- id, profile_id, deadline_reminders_enabled (default true), wellbeing_checkin_reminders_enabled (default true), days_before_to_remind (array of integers, default [14, 7, 3, 1])

Add a Vercel Cron job (or Supabase scheduled function) that runs daily at 8am user's local time:
1. Query for upcoming deadlines (compliance alerts, pre-departure actions, settling-in tasks).
2. For each user with email preferences enabled, identify deadlines within their reminder window.
3. Send email via Resend using a templated email (subject: "GoMate: Action needed in X days for [deadline]", body: deadline details + link to relevant page in app).

Email templates:
- deadline_approaching.html — for upcoming compliance deadlines
- weekly_summary.html — Sunday morning summary of progress and upcoming work
- wellbeing_checkin.html — gentle nudge to check in if user hasn't in 7+ days
- new_deadline_added.html — when research adds new compliance items the user should know about

Step 5: Document templates library.

Build lib/document-templates/ directory with template files for:
- power_of_attorney_property.md — POA for property management in origin while away
- power_of_attorney_tax.md — POA for tax filing in origin
- lease_termination_notice.md — letter to landlord with proper notice
- utility_cancellation_notice.md — generic utility cancellation
- employer_relocation_request_letter.md — letter to employer requesting relocation support
- school_transfer_request.md — letter requesting school transfer documents from current school
- doctor_records_transfer_request.md — letter to GP requesting medical record copy
- pension_continuity_request.md — letter to pension fund for continuity options

Each template has placeholder variables that get filled in from user profile (e.g., {USER_NAME}, {ORIGIN_ADDRESS}, {DESTINATION_ADDRESS}, {MOVE_DATE}, {LANDLORD_NAME if known}).

Build app/api/document-templates/[type]/route.ts. GET returns the template with user's profile data filled in. User downloads as PDF or copies to clipboard.

In the Checklist page Documents tab, add a "Templates" sub-section listing available templates. User selects template → personalized version generated → download/copy.

Step 6: Surface all this in the dashboard.

Add small status tiles or badges on dashboard:
- Document upload status: "8 of 14 documents uploaded"
- Email preferences: "Reminders enabled" (link to settings)
- Wellbeing trend: small mood indicator if recent check-ins trend down

Run pnpm tsc --noEmit. Confirm all migrations apply. Send test email if you can reach Resend API.

When you're done, walk me through:
1. The new plan consistency engine flagging POSTING_EXCEEDS_A1_LIMIT for a 30-month posting profile
2. Stage-aware wellbeing response when user is in pre-departure stage and reports "overwhelmed"
3. Document upload for a passport photo, with the new user_documents row created
4. Email reminder triggered for a 7-days-out compliance deadline
5. Document template generation for "lease_termination_notice" filled with user's profile data
```

---

## Prompt 9.8 — Team Activity Feed: make the consultant team visible on the dashboard

```
This is the single biggest visual signal that GoMate is a consultant team, not a chatbot. Without it, the multi-agent architecture is invisible once research finishes. Build a persistent dashboard module that surfaces your team at all times.

Build components/team-activity-feed.tsx. Renders at the TOP of the dashboard, between the hero banner and the stat cards. Always visible, regardless of plan stage.

Two parts side by side (or stacked on mobile):

PART 1 — Specialist roster (horizontal avatar row, scrollable on mobile)

Show all specialists as small circular avatars (40-48px) with name underneath. Each specialist has a status indicator dot in the bottom-right corner of the avatar:
- green pulse: actively working right now (research dispatched within last 60s, or audit row from last 5 minutes)
- green steady: completed work for current stage; results visible in dashboard
- yellow: dispatched but waiting (e.g., waiting on user input, blocked by dependency)
- gray: not relevant for this profile (e.g., Pet Specialist if pets="none", Posted Worker Specialist if no posting)

The full specialist roster (verify against existing prompts 3.2, 3.3, 9.6 — use the actual count after those prompts have been built):
- Always-run (from 3.2): Visa Specialist, Tax Strategist, Cost-of-Living Analyst, Housing Researcher, Cultural Adapter, Documents Specialist
- Conditional (from 3.3): Posted Worker Specialist, School Specialist, Pet Specialist, Family Reunion Specialist
- New consultant-grade (from 9.6): Insurance Specialist, Departure Origin Specialist, Goods Shipping Specialist, Driver's License Specialist, Embassy Registration Specialist, Family Visa Cascade Specialist, Healthcare Continuity Specialist
- Plus orchestration roles: Coordinator, Synthesizer, Critic (these get a smaller "ops" badge style to distinguish from domain specialists)

Hover/tap on any avatar → tooltip showing: specialist title, what they specialize in, current status, last action timestamp (e.g., "Visa Specialist · scrapes Migrationsverket and visa-route APIs · last action: 12 minutes ago").

PART 2 — Recent activity feed (vertical list, last 5 actions)

Pulls from agent_audit table (created in Prompt 1.4). Each row shows:
- Specialist avatar (small, 24px)
- Action description in plain English (e.g., "Visa Specialist updated Migrationsverket processing time data", "Documents Specialist confirmed your Filipino birth certificate needs apostille via DFA Manila")
- Source citation if applicable (e.g., "from migrationsverket.se/work-permit") rendered as a small clickable link
- Timestamp, relative format ("just now", "12 min ago", "3 hours ago", "yesterday")

If no recent activity (fresh profile, nothing dispatched yet): show empty state "Your team is standing by. Start your relocation plan to get them working."

Click any activity row → opens the audit drawer (existing from Prompt 4.3) filtered to that specialist's audit history.

Build app/api/team-activity/route.ts. GET endpoint takes profile_id (or uses session user). Returns:
- specialists[]: { id, name, specialty, role: "domain"|"orchestration", status: "active"|"completed"|"waiting"|"not_relevant", lastActionAt: timestamp|null, lastActionSummary: string|null }
- recentActivity[]: { id, specialistId, description, sourceCitation: { url, label } | null, timestamp }

Status computation logic (in lib/team-activity.ts):
- "active" if any agent_audit row in last 5 minutes
- "completed" if has audit rows but none in last 5 minutes AND profile stage allows that specialist's work to be done
- "waiting" if dispatched but blocked (check coordinator state)
- "not_relevant" if specialist's trigger condition is false (e.g., Pet Specialist when pets="none")

Polling: feed refreshes every 10 seconds via SWR or React Query. During an active research run, this means user sees the team light up in real time.

When you're done:
1. Show the dashboard with the team feed visible above the stat cards
2. Show the specialist roster with at least 4 different status states (active, completed, waiting, not_relevant)
3. Show the recent activity feed with at least 3 entries pulled from real audit data
4. Click an activity row and verify it opens the audit drawer correctly filtered
5. Trigger a research run and watch the team feed light up in real time as specialists work
```

---

## Prompt 9.9 — Prioritization Agent + "This week's priorities" dashboard card

```
Build a Prioritization Agent that proactively tells the user what to focus on THIS WEEK. This is the single biggest signal that GoMate is proactive, not reactive — a consultant pushes priorities to you, a search tool waits for you to ask.

Build lib/agents/prioritizer.ts using the agent infrastructure from Prompt 1.5.

Inputs (read from existing tables):
- profile data (current stage, target_arrival_window, household composition, all schema fields)
- plan_consistency_warnings (active warnings from validatePlanConsistency)
- pre_departure_actions (open actions with deadlines, from Phase 5)
- settling_in_tasks (if stage = "arrived", open tasks with deadlines)
- document_statuses (documents not yet collected, expiring documents)
- agent_audit recent activity (what's been worked on, what's pending)

Outputs: top 3 priorities for THIS week with structured fields:
- title: action-oriented imperative ("Submit your A1 application this week", "Apostille your marriage certificate now", "Confirm your school admission offer for Linus")
- reasoning: 1-2 sentences explaining why this is top priority — must combine deadline urgency, dependency chains, and risk
- ownerSpecialist: which specialist agent flagged or is responsible for this
- relatedAction: link to existing pre_departure_action.id, settling_in_task.id, or document key — so "Take action" button can navigate
- urgencyLevel: "critical" | "important" | "recommended"
- estimatedTimeToComplete: human-readable ("20 minutes", "1 hour", "this week")

Algorithm — deterministic ranking, then LLM polishes copy:

Step 1 (deterministic): score every open action by a weighted formula:
score = (deadline_proximity_score * 3) + (dependency_blocking_count * 2) + (risk_severity_score * 4)
where:
- deadline_proximity_score = max(0, 100 - days_until_deadline) — capped at 100; missing deadlines score 100
- dependency_blocking_count = count of actions blocked by this one
- risk_severity_score = 100 if "critical" plan_consistency warning, 50 if "warning", 10 if "suggestion", 0 otherwise

Step 2 (deterministic): take top 5 candidates by score.

Step 3 (LLM, Sonnet 4.6): pass top 5 with profile context to LLM with this brief:
"Pick the top 3 priorities for the user's coming week. Write each title as a clear imperative. Write reasoning that weaves together why (deadline urgency + downstream impact + risk). Ensure variety — don't pick 3 visa-related items if other categories also rank well. Return as JSON array of 3 priority objects."

Step 4 (deterministic): return structured output.

Build app/api/prioritize/route.ts. POST runs the prioritizer for the session user's active profile. Caches result in a new prioritization_cache table (id, profile_id, computed_at, priorities jsonb) for 24 hours. Returns cached result if fresh.

Add migration for prioritization_cache table.

Build components/this-weeks-priorities-card.tsx. Renders prominently on dashboard — best position is RIGHT under the team-activity-feed (from 9.8), before the stat cards. Card title: "This week" or "Your team's recommended focus". Each of the 3 priorities renders as a row showing:
- Urgency badge (red "Critical" / orange "Important" / blue "Recommended")
- Owner specialist avatar + name (small, 24px)
- Title (large, bold)
- Reasoning (smaller, 1-2 sentences)
- Estimated time
- "Take action" button → navigates to the related action's page (pre-departure detail, settling-in task detail, or documents tab)

Add a "Refresh priorities" link/button on the card (small, top-right) so user can manually re-run prioritization after completing something. Calls POST /api/prioritize with force_refresh=true.

Optional cron (defer to post-buildathon if time-pressed): vercel.json daily cron at 6am UTC that calls /api/prioritize for every active profile. For buildathon, manual refresh + initial-load auto-compute is enough.

When you're done:
1. Show the dashboard with "This week's priorities" card visible right under team-activity-feed
2. Show 3 priorities ranked correctly for the Roselle profile (Filipino → Sweden, dependent visa role, no posting, no kids) — likely top: visa documents, apostille from DFA Manila, target arrival window confirmation
3. Show 3 priorities for the Axel posting profile (Germany → Sweden, 14-month posting, 2 kids, wife joining) — should differ significantly, with A1 / Posted Worker Declaration / school admissions likely top
4. Click "Refresh priorities" and verify rerun works (returns fresh result, cache updated)
5. Click "Take action" on one priority and verify navigation to the correct related-action page
```

---

## Prompt 9.10 — Set all features free + polish pass

```
Step 1: Set all features free for buildathon.

Update lib/gomate/tier.ts so getUserTier() returns the highest tier ("Pro Plus" or equivalent) for ALL users regardless of subscription.

Update components/tier-gate.tsx to always pass through (no gating).

Update components/upgrade-modal.tsx to be hidden during the buildathon (never trigger).

Update lib/gomate/usage-guard.ts to remove rate limits during the buildathon (we have unlimited credits).

Step 2: Polish pass. Walk through every page of the app:
- Loading states that say what's happening, not "Loading..."
- Error messages that explain what to do
- Hover states on every interactive element
- Mobile breakpoints that look broken
- Color contrast issues
- Typography inconsistencies
- Empty states with help text
- Animations that feel intentional, not janky

Walk through the full Spine end-to-end as a real user (don't skip any steps):
1. Land on /
2. Start onboarding (mascot flow)
3. Answer questions including ones that need clarifying follow-up
4. Verify Question Director only asks fields from the schema (test it tries to ask about "favorite color" — it shouldn't)
5. Watch onboarding complete with celebration animation
6. Click "Generate my plan" button
7. Watch research happen with agent panels — verify Posted Worker Specialist fires for posting profile, doesn't fire for non-posting
8. Read resulting guide with citations — click some to verify they go to real official sources
9. Click some audit icons on dashboard fields and research outputs
10. View dashboard with profile-aware cards — verify schools-card and posted-worker-card visibility per profile
11. Click "Generate my pre-departure checklist" button
12. Look at pre-departure timeline — verify A1 certificate action exists for posting profiles, apostille chains for Filipino dependent
13. Mark some pre-departure actions complete
14. Try comparison mode at /compare
15. Open agent debug drawer
16. Click "I have arrived" → settling-in DAG generates
17. Try post-arrival chat with smart suggestion chips
18. Verify settling-in DAG has correct deadlines (7-day Anmeldung in Sweden as urgent)

Fix every glitch encountered. Don't just list them.

Run the Spine three times in a row from scratch (fresh browser session each time). It must work every time.

When you're done, tell me everything you fixed and everything you couldn't fix.
```

---

# Pre-flight checklist (do before starting buildathon timer)

1. Capture v1 baseline. Record 90-second screen video of v1 in action with broken extraction visible. Type "I'm 30 years old, planning to move to Sweden for work next year with my wife and two kids" as your name answer. Show dashboard fill with garbage. Take screenshots. Save with timestamps. This is the "before" half of the 33%-weighted Progress score.

2. Note v1 line counts and feature inventory. Run wc -l app/**/*.tsx components/**/*.tsx lib/**/*.ts and save as screenshot. Take screenshots of every dashboard card v1 has. Save the API route inventory.

3. Pick three primary user countries from existing seed: Sweden (Roselle's destination, your story), Spain (digital nomad lifestyle), Germany (work relocation). Verify their official-sources URLs are alive. Fix any broken entries before timer starts.

4. Have your real Filipino-Swedish profile ready as the headline user profile. Roselle: Filipino citizen, moving to Sweden, partner is Swedish, settle purpose, dependent visa role. Also have the Axel-Germany-Sweden-posting profile ready as the second walkthrough profile (mirrors the Spine input).

5. Set up the buildathon Replit project. Clone GoMate into a new Replit project. Don't reuse an existing Replit. Fresh project, fresh timestamps.

When all five are done, start the timer. Then paste Prompt 1.1.

---

# Story arc reference (for video and submission, not for Replit)

Use language from your existing pitch_en.md. Don't invent new copy.

Video (90 seconds):

0-15s: "Min fästmö är från Filippinerna. Jag är från Sverige. För att vi ska kunna leva tillsammans måste en av oss flytta över världen. Jag trodde det skulle vara enkelt. Det var det inte."

15-35s: Show v1's chat with broken extraction filling birth_year with literal string. "Jag byggde GoMate. 11 build phases. Compliance engine. 65-field profile. Web-grounded research. Live till 99%. Men en sak höll oss tillbaka från lansering."

35-65s: "Idag på Replit Buildathon transformerade Replit Agent och jag GoMate till v2. Multi-agent extraction. Multi-agent research. Visible agent panels. Audit trail per fält. Ny pre-departure phase. Posted-worker-stöd för A1, CoC, PWD." Show agent panels working live with Visa Specialist scraping Migrationsverket, Tax Strategist computing exit tax, Posted Worker Specialist building A1 timeline.

65-85s: Same input as 15-35s. Show v2 correctly extracting birth_year=1995, destination=Sweden, posting_or_secondment detection, with audit trail showing agent steps. Click a citation to verify it goes to Migrationsverket. "GoMate v2. The tool Roselle and I needed. Now actually working."

85-90s: "GoMate. The operating system for cross-border life. Built with Replit Agent in 24 hours. Now live."

X / Social post:

```
My fiancée is from the Philippines. I'm from Sweden. To live together, one of us has to relocate across the world.

Moving abroad isn't an information problem. It's an execution problem. Anyone can google a visa. But what about A1 certificates for posted workers? Posted Worker Declarations? 7-day population registration? The order things have to happen?

For 11 build phases I've built GoMate — a compliance OS for relocation. Web-grounded research, 65-field profiles, DAG-based task graphs. But one bug kept us from launch: AI extraction hallucinated.

In 24h on @replit Buildathon, Replit Agent helped me rebuild the core with multi-agent architecture. Extraction. Research. Synthesis. Visible agent panels. Audit trail. Posted-worker support with A1/CoC/PWD. All ground-truthed against official sources.

This is GoMate v2. The tool Roselle and I needed. Now live.

[link]

Built with @replit. Powered by @AnthropicAI. Grounded by @firecrawl_dev.
```
