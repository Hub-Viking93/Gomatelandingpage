import React from 'react';

/**
 * DEPRECATED: This component is no longer used.
 * API Key is now managed via environment variables (.env).
 * This file is kept to prevent import errors during transition but renders nothing.
 */
const APIKeyInput = () => {
  return null;
};

export default APIKeyInput;